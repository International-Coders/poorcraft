# STATE
loop_count: 362
current_milestone: POORCRAFT 3D P3D-002 — versioned headers + refusal law
last_done: "loop 362 P3D-002 (P3D-000 stage): the format has a LAW now, not just a magic number. pc3d_core::header defines the 16-byte fixed-layout file header — magic(4) | epoch u32le | world/save/content/protocol u16le each — with hand-rolled byte-exact encode/decode (no serde; the layout IS the contract, pinned against a hard-coded byte vector) and open_decision(bytes, supported) layered OUTSIDE the P3D-001 magic guard: Accepted / TooShort / ForeignFormat / UnknownEpoch / Newer{section,file,supported} / Older{...}, every refusal carrying both exact numbers and a human line (newer file -> update the game; older file -> this build cannot downgrade). Policy at epoch 1: any section mismatch refuses — no migration tooling exists, nothing is guessed (D-002). Six header tests: byte-layout stability, round-trip (incl. trailing payload), per-section newer/older refusal across all four axes with first-offender determinism, unknown-epoch rejection, guard layering (foreign magic refused BEFORE version logic), decision determinism. poorcraft3d --format prints the layout, supported versions, and wire bytes. Task contract at docs/POORCRAFT-3D/contracts/P3D-002.md filled first. No real files consume the header yet — P3D-102 (patch store, atomic save) wires it in."
next_task: "P3D-003 — deterministic clock, command envelope, event journal, seed streams, and replay hash harness (docs/POORCRAFT-3D/16-IMPLEMENTATION-WORK-BREAKDOWN.md): the P3D-native primitives, designed greenfield — fixed-step clock, sequenced commands, append-only journal with stable hashing, per-subsystem seed streams, and a replay-hash test harness. Fill the task contract first. NOTE: BETA-FOUNDATION track (original game) stays parked at B04."
build: GREEN
tests: 474 passed / 0 failed (root workspace, loop 360) + 11 passed / 0 failed (poorcraft3d workspace, loop 362)
last_screenshot: shots/vistest_biome_transitions.png
blockers: "none"
