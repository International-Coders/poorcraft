# STATE
loop_count: 422
current_milestone: WT-001 IN CODE: the New World seed preview harness — pc3d_world::seed_preview (pure authority: resolve/reroll/preview with spawn-safety + biome census + real river/stronghold hints) + the two-panel New World screen (map, safety, biomes, hints, resolved seed, RANDOM) + --ui-seed-preview-shots / --seed-preview / make p3d-seed-preview
last_done="WT-001 (loop 422): the seed preview harness IN CODE. (1) PURE AUTHORITY pc3d_world::seed_preview: resolve_seed (numeric exact / text FNV-1a / empty+entropy), reroll_seed (never returns the current seed — the old LCG %100000 reroll could collide), preview_seed over a 32x32-region window: exact biome census, spawn spiral with the full law (not water, above sea, slope <2.6m/16m, >=6/9 walkable neighbors, not inside a capital-keep footprint), REAL feature hints (river distance from RiverGraph, stronghold ground from footprint_fits; settlement an honest placeholder), 96x96 CPU map (biome palette + relief + river overlay + spawn marker); the JSON sidecar lives in ui::seed_preview_json (pc3d_world stays serde-free). (2) THE SCREEN: New World is two panels — form + WORLD PREVIEW (preview_map image element, spawn_safety, biome_summary, feature hints, nw_seed_resolved); RANDOM button; REROLL/RANDOM capped to 15 digits (a 19-digit seed ran under the map — the overlap check caught it); form values truncated to their columns; CREATE disabled + Enter/click blocked when the preview is unsafe; layout dump exposes button states. (3) APP: refresh_ui recomputes when stale (empty seed = placeholder, never a fake map); CaptureOutcome carries the preview. (4) PROOFS: 9/9 authority laws (WT-001 matrix verbatim), 36/36 ui tests, windowed battery 6/6 PASS (empty/typed/rerolled/random/deck-1280x800/warning-seed-8) at p50 7.47ms avg 133fps with sidecars + seed_preview_pixel_report.json, map NOT noise (70-72% neighbor agreement), make p3d-ui-shots 13/13 on the new layout, p3d 596/596 (+16), root green; DMG rebuilt + stamped after this commit. CONCURRENT-SESSION NOTE: the samurai-cut commit swept this session's in-flight ui.rs without the seed_preview module — HEAD was briefly red; this commit restores green. DEFERRED: settlement hint placeholder (no siting authority yet), one world type, recompute one frame after seed change, random-shot window happens unsafe (legit warning state), no map zoom/pan. Previous: WT-006/007 docs packs + the samurai-cut fix (quad-coverage law). The owner play-pass gate stands."
next_task: "WT-001 LANDED (loop 422). Next per the WT queue: WT-002 semantic asset factory lab in code (inspection/factory over the semantic asset tags + the 300-variant batch), then WT-003 observatory (local MCP-style tools, state exports, input replay), WT-004 feature slices, WT-005 asset prompt atlas import pipeline — interleaved with the other session's docs packs (WT-008 in flight). Keep the owner play-pass gate for the stamped DMG as the separate manual gate."
build: GREEN
tests: "p3d 596/596 (+16: 9 seed_preview authority laws, 4 WT-001 ui tests, quad-coverage + load-world label laws from the concurrent session), root suite green, ui-shots 13/13 incl. row-shear + quad-coverage laws, seed-preview battery 6/6 with sidecars + pixel report at p50 7.47ms, headless --seed-preview export verified"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/seed_preview_typed.png (WT-001: the New World screen with the live preview map, spawn-safety line, biome summary and CREATE gating; 6-shot battery + seed_preview_pixel_report.json)
blockers: "none for loop 418"
