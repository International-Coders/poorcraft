# STATE
loop_count: 418
current_milestone: THE SAMURAI CUT fixed: the UI layer + HUD quads drew as ONE TriangleList triangle (bottom-right half of every menu sliced off); now 6-vertex quads with a byte-exact quad-coverage proof law, an sRGB view fix, and two uncovered label overflows repaired.
last_done="THE SAMURAI CUT (loop 418): reproduced the owner's 'diagonally cut' menus in the proof captures and root-caused it to a SECOND diagonal-cut bug beside the fixed row-shear bug: set_ui_layer and the HUD debug-line quads uploaded 4 vertices and drew draw(0..4) on TriangleList pipelines = ONE triangle, so everything below-right of the screen's top-right->bottom-left diagonal showed raw world while every canvas-side check and the row-shear law stayed green. Fixed with 6-vertex two-triangle quads; added the quad-coverage LAW (verify_ui_captures now requires the PRESENTED frame to reproduce the canvas's opaque ink byte-exact inside every required element rect — ui-shots prints 'quad coverage N/N'); fixed the exposed double-gamma (UI canvas now sampled through an Rgba8UnormSrgb view on sRGB targets — the UI was washed out on the real screen) and two label overflows the cut had hidden (quality steppers now '-'/'+', load-world labels fit_to_width at the drawn glyph scale). Renderer::offscreen no longer defaults the dev debug line on (5 world-probe tests had been calibrated against the torn half-quad). EVIDENCE: ui-shots 13/13 incl. both 1501x801 legs at 100% coverage, human-inspected whole menus + full hotbar; p3d 592/592 (+2 tests); gates 10/10; journey 7ab2295dafa0ec24 + smoke dd019eca900f5a61 unchanged; DMG rebuilt from the committed tree and verified from the mount."
next_task: "Implement WT-001 through WT-007 in code: seed preview, semantic asset factory, observatory tooling, feature matrix slices, asset prompt atlas/import pipeline, continuous runbook, and GPU marker/capture cookbook. Start with playability recovery plus seed/world preview, then house/NPC/forge semantic route, first accepted asset batch, and GPU marker audit. Keep the owner play pass gate for the stamped DMG as a separate manual gate."
build: GREEN
tests: "Loop 418: p3d 592/592 green (cargo test --manifest-path poorcraft3d/Cargo.toml); --ui-shots 13/13 PASS with quad coverage 100% per scene; p3d-visual-gates 10/10 PASS; journey digest 7ab2295dafa0ec24 unchanged; smoke digest dd019eca900f5a61 unchanged."
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/screen-proofs/ (real-screen capture of the MOUNTED post-fix DMG: title menu whole, no diagonal cut; plus 13/13 harness captures with quad coverage 100%)
blockers: "none for loop 418"
