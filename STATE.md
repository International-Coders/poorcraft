# STATE
loop_count: 423
current_milestone: WT-002 IN CODE (slices 1/2/4): the semantic asset factory lab — contracts parse + the rejection law bites (doorless house / talkless NPC / inputless forge refused), the 7-asset starter batch registered against real artifacts (2 new GLBs: chest + ore_node; every building gained the interior socket), and windowless inspection sidecars whose anchor lists are verified against the GLB bytes (7/7 PASS via --asset-sidecar / make p3d-asset-sidecars)
last_done="WT-002 (loop 423), slices 1+2+4 of the semantic asset factory lab. SLICE 1 CONTRACTS: pc3d_assets::semantic embeds gameplay_asset_catalog/asset_affordance_schema/asset_factory_queue, typed parsers, and missing_affordances() enforcing the category laws (anchor of the required kind, gameplay key, or collision/nav field; lawless categories still require the minimal metadata surface — nothing is silently accepted). SLICE 4 FIRST BATCH: all seven queue-required assets registered against real artifacts — starter_house->module.house_croft, talkable_villager->npc sim authority (identity/schedule/state), working_forge->the journey-proven machine chain, openable_chest->NEW prop.chest GLB (timber chest, iron bands, open lid; open/lid/inventory anchors; 120/200 tris), harvestable_ore_node->NEW prop.ore_node GLB (granite + ember-quartz veins; harvest anchor; 198/400), map_marker_set->banner_sign (gained inspect anchor), hud_icon_set->the UI painter; every building GLB gained the schema-required interior socket. SLICE 2 SIDECARS: pc3d_render::inspect loads the REAL GLB (bounds/triangles/LODs/materials from lod0), cross-checks EVERY registry anchor against the GLB sockets — a promised anchor missing from disk is a NAMED FAILURE — and emits the 16-field inspection_export_contract sidecar; --asset-sidecar all -> 7/7 INSPECTION PASS on disk. EVIDENCE: pc3d_assets 32/32 (+5 semantic tests incl. the rejection matrix and batch-completeness vs the queue list), pc3d_render inspect 4/4 (house door+nav+interior proven from GLB bytes with in_glb=true; chest open + ore harvest anchors; a sabotaged ghost anchor fails and is named), inventory guardrail caught the 2 new GLBs and they were registered (324 files), assetgen deterministic regen under budgets, full suites re-run green after the guardrail fix. DEFERRED (their own queue entries): slice 3 beauty/wireframe/anchor-overlay captures (WT-003 observatory), slice 5 semantic playtest route (talk/forge interactions), slice 6 GPU pass markers (WT-007). Previous: WT-001 seed preview (loop 422)."
next_task: "WT-002 slices 1/2/4 LANDED (loop 423). Next per the WT queue: WT-003 game observatory in code (local MCP-style tools, runtime state exports, screenshot/wireframe/anchor-overlay captures, input replay, evidence bundles) — which also completes WT-002 slice 3; then the semantic playtest route (WT-002 slice 5) once talk/forge interactions land. Keep the owner play-pass gate for the stamped DMG as the separate manual gate."
build: GREEN
tests: "pc3d_assets 32/32 (+5 semantic), pc3d_render inspect 4/4, inventory guardrail green (324 files), asset-sidecars 7/7 PASS on disk, full p3d + root suites green after the guardrail registration"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/seed_preview_typed.png (WT-001 stands); WT-002 evidence is the inspection_*.json sidecars (windowless slice) — the capture slice (WT-003) adds the PNGs
blockers: "none for loop 418"
