# STATE
loop_count: 453
current_milestone: THE LETHAL FALL WAKES AT THE PLAZA: the fall law's 0.5-health plaza recovery has its windowed route — a 14 m pit empties the body's health and the recovery wakes it at the plaza's heart ON the plaza ground, health EXACTLY 0.5, food halved, under its own toast (whose em-dash finally has a glyph)
last_done="The plaza recovery, live (loop 453, POORCRAFT 3D): closed STATE's next_task item (1) — the fall law's lethal branch (health emptied -> wake at the plaza, health 0.5, food halved, 'YOU FELL — RECOVERED AT THE PLAZA') had unit laws but no windowed route. (1) THE ROUTE (make p3d-recovery, route_plaza_recovery): the walk-off's own spot finder (flat, flora-free, vine-free with margin — a strand catch would erase the drop) stages a cell near the plaza; one -14 m EditSurface pits it under the standing body; the arc lands ~16.5 m/s, the impact law costs ~1.15 health — MORE than the body carries — and the recovery fires. Verdict bands: rim standing on the LIVE pre-dig ground at full health/food; a mid-fall pose between pit floor and rim (two record chances, either proves the air beat); recovered XZ at the plaza center, feet ON the LIVE plaza-ground answer, health EXACTLY 0.5 (±0.001), food exactly (before*0.5).max(0.3); a later record pins the body STILL there at 0.5 (no second fall; 0.5 food is not 'well fed', no regen); the recovery toast present and the plain FELL toast ABSENT; captures rim/air/plaza_early/plaza/final all differing. (2) THE PREDICTION THE RUNTIME DISPROVED, honestly: orientation statics said the recovery 'keeps the pit-floor Y' and would plant the body 14 m UNDER the plaza (the walk's feet+2-bounded query answering nothing that deep, is_unsupported false when the ground is above the feet). The first run against UNCHANGED code PASSED — the walk's per-frame ground snap (pos[1] - g <= SUPPORT_GAP_M is trivially true for any rise, the same predicate 449 fixed for axis MOVES) lifts the body onto the plaza in one frame; the branch is correct in practice, so NO pose change shipped — the route now laws the outcome. Recorded observation: surface.rs ground_at takes _from_y and DISCARDS it on the streamed path; if the bound is ever enforced, make the recovery's placement explicit first (next_task carries this). (3) THE GLYPH, a proof-found fix: the wake-up capture showed 'YOU FELL — RECOVERED AT THE PLAZA' rendering as 'YOU FELL   RECOVERED…' — the em-dash was missing from the pixel font and unknown chars silently fall back to space (the pre-existing 'FELL — HEALTH x%' toast had the same hole). Added the em-dash glyph (a full-width bar, original) + ink law the_em_dash_rasterizes_with_ink (also pins it distinct from the hyphen) — pc3d_render 223 -> 224; both fall toasts now read with their dash. (4) EVIDENCE: p3d workspace 681 green / 0 failed (pc3d_render 224, pc3d_world 267); make p3d-recovery PASS x2 identical + comparator PASS ('fell 54.49 -> pit floor 40.49; woke at the plaza (384.5,128.5) feet 55.19 (plaza ground 55.19); health 100% -> 50% EXACT, food 1.00 -> 0.50'; p50 ~20.8 ms); all five captures INSPECTED (rim at full bars; the shaft mid-fall with THE GROUND GIVES WAY; the wake-up with half bars + inked-dash toast; the fading repeat; final stable at half health by the plaza forge). (5) REGRESSION: make p3d-walkoff PASS x2 + comparator (56.19 -> 51.19, 67%; walk_landed INSPECTED — 'FELL — HEALTH 64%' now reads with its dash); make p3d-dig PASS x2 + comparator (pack PACK EMPTY -> PACK WOOD 1 · SOIL 1, unhurt); make p3d-playtest PASS x2 + comparator, bundle digest 05c46411869a857c on this loop's dev-stamped builds — recorded honestly: the digest covers the bundle JSON (including the compile-time PC3D_BUILD stamp), not capture pixels, so 452's 'RE-BASELINED 1d37f62c3801ae20' was a PC3D_BUILD-stamped rebuild's value; play_fall_landed INSPECTED ('FELL — HEALTH 33%' with its dash over the open forge + the pack line); make p3d-people PASS (12 NPCs, 7 draws, 92 instances, frozen-stride motion 0.66%); p3d-visual-gates ALL 10 PASS; p3d-smoke OK (digest dd019eca900f5a61 UNCHANGED — headless, no font path); idle-upgrade-check PASS; the six windowed_wild_*.png dirties belong to an earlier session's p3d-wilderness run and are deliberately NOT staged. (6) PERF: no claim, honestly — the route adds work only while it runs (one bounded staging scan, a -14 m edit, five captures); the live walk/stream/crowd paths are untouched (the em-dash is one more 35-pixel glyph row in an already-dirty-frame raster); host shared (route p50 20.0-23.4 ms), no bench per the 445-452 precedent; the quiet-host re-read stays queued (eight loops). LORE: canon touched: none — a consequence branch of the existing fall law over the unnamed settlement's plaza anchor; no faction, place, event, term, NPC, item, or spell data; no identity assigned. World expression: lethal falls in Valdenmoor do not end the journey — the body wakes at the settlement's heart, wounded (health 0.5) and hungry (food halved), and the toast says so legibly. Migration: none (in-memory route + one glyph; no persisted field, save format, or proof schema change). Deferred honestly: Space jump-off from a hang (next_task item 1); the walk-snap query-bound observation (recorded above); the quiet-host deck-bench re-read + the bench contention guard; is_water_layer/CTM-strip audit; guardian chronicle re-fire; multiplayer routing of terrain edits; geode pairing."
next_task: "Carry the list (p3d): (1) Space jump-off from a hang (unit-lawed only; the climb route's strand owns the body — no windowed route frames the hop-off). Carried deferrals: the quiet-host deck-bench re-read (queued by 446..453) + the bench contention guard; is_water_layer/CTM-strip audit; guardian chronicle re-fire; multiplayer routing of terrain edits; geode pairing (447's keepers are root-workspace lore). Recorded this loop: surface.rs ground_at discards its _from_y bound on the streamed path (the recovery's one-frame lift onto the plaza rides that predicate; the route law pins the outcome) — if the bound is ever enforced, make the recovery's placement explicit first."
build: GREEN
tests: "p3d workspace 681 green / 0 failed (pc3d_render 224 = 223 + em-dash ink law; pc3d_world 267); make p3d-recovery PASS x2 + comparator PASS (fell 54.49 -> pit floor 40.49; woke at the plaza feet 55.19 = plaza ground; health 50% EXACT, food 0.50; captures inspected); make p3d-walkoff PASS x2 + comparator; make p3d-dig PASS x2 + comparator; make p3d-playtest PASS x2 + comparator (bundle digest 05c46411869a857c on dev-stamped builds); make p3d-people PASS (12 NPCs, 7 draws, 92 instances, motion 0.66%); p3d-visual-gates ALL 10 PASS; p3d-smoke OK (digest dd019eca900f5a61 unchanged); idle-upgrade-check PASS"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/recovery-a/route_plaza_recovery/recovery_plaza_early.png (the toast YOU FELL — RECOVERED AT THE PLAZA with the inked em-dash over the plaza, health and food bars at exactly half — from this loop's green make p3d-recovery run)
blockers: "none; a windowed route over the existing fall law + one font glyph — no save format, worldgen, or authority change"
