# STATE
loop_count: 303
current_milestone: P23-urgent-fixes
last_done: "P23: urgent fix pass — input defenses (close_ui clears stale chat input that could hold an invisible Chat screen eating every key/click; Escape now closes the Pause menu; cursor-grab fallback keeps clicks/mouse-look alive when the OS refuses the grab; the click that re-locks input now also acts; F3/LOREFORGE_DEBUG_INPUT overlay exposing ui_open/locked/playing/keys). Developer console (` or /): 20 commands (time set, give, tp, seed, weather, fly, heal, feed, kill, spawn, clear, waypoint add/list/remove, say, fps, rt, save, slots, load, new) with TAB autocomplete cycling, up/down history, help — pure Command parser unit-tested. Real per-world seeds: WorldGen exposes its u64 seed, per-channel splitmix64 hashing (no more u64->i32 truncation collisions), seed.dat persisted per slot, streamer restarted on world switch (was a latent bug), server persists its own seed and sends the true value in Welcome. Natural biome transitions: fractal 3-octave climate at lower frequency + contrast stretch, domain warping (±34 blocks) so borders follow organic curves, fine dithering (±0.045) that turns hard threshold lines into mixed transition bands; biome_from stays pure (tests intact), coverage test widened. Multiple save slots: worlds/<slot>/ with meta.dat (name/type/seed/updated), title menu reordered (Play — <slot> / New World submenu / Load Game / Multiplayer / Settings / Quit), slot picker with Load/Delete-confirm/Create, pause menu gains Save Now + Load Game + Quit to Title; worlds/default auto-migrated to World_1 with its seed (verified live); load_world() reboots state mid-session (tested: meta/seed/chunks roundtrip, migration). UI scaling: egui zoom = user scale × native DPI × viewport factor (720p reference) so everything scales with window size AND pixel density; app bundle NSHighResolutionCapable. vistest: console_preview scene added (windows-only harness quirk found: plain Areas never render in the 2-pass headless harness — previews use frameless windows). 140 -> 149 tests; 20/20 proofs pixel-verified; game smoke-tested with migration confirmed; runtimes rebuilt"
next_task: "user playtest of the input fixes; then backlog (sound, beds/doors, A*, PT-BR)"
build: GREEN
tests: 149 passing
last_screenshot: shots/vistest_console_preview.png
blockers: "github push still blocked by PAT missing workflow scope"
