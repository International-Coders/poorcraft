# STATE
loop_count: 458
current_milestone: THE BENCH GUARDS ITS HOST: the deck-bench carries its own contention evidence (a calibrated CPU probe bracketing every tier run, a band law asserted before the report is written), and the twelve-loops-queued quiet-host re-read landed with an A/B that pins the record's +4-6% shift to host drift, not code
last_done="The bench guards its host (loop 458, POORCRAFT 3D): closed STATE's next_task item (1) — the quiet-host deck-bench re-read queued since loop 446 — plus 445's own deferred bench contention guard. (1) THE GUARD (pc3d_render::deck): cpu_probe_ns, a fixed 400k-step deterministic float workload (~2.7 ms per reading, calibrated from the release binary: 100k steps read 0.67 ms with a 1.2% start/end spread; 4x averages scheduler transients; pure std, no new deps), read at each tier run's start and end; the readings ride the CSV sidecar (two new columns) and the report's 'host CPU probe' bullet; the pure probes_within_band law (slowest <= fastest x 1.4, a zero/missing reading never counts as quiet) is asserted by deck_report BEFORE the report is written — a contended run writes no report and exits with the readings printed (contamination is a first-class failure, the exact failure mode of 445's discarded first attempt). 4 new unit laws; pc3d_render 229 -> 233; p3d 686 -> 690. (2) THE RE-READ: make p3d-deck-bench PASS on a quiet host (1-min load 3.3-4.1 through the run; probes 2.68M ns with a 0.7% spread, band x1.4 held): low 7.18 / mid 12.92 / high 13.06 ms p50 (p95 8.58/14.26/14.09; meshed 176/402/603; GPU 5140/18886/27587 KB; flora 605; setl 1048 tris; crowd 92 — every work counter byte-identical to 445's record) vs 442/445's 6.85-6.89/12.16-12.30/12.44-12.56: a uniform +4-6%. (3) THE A/B THAT DECIDES THE SHIFT: the loop-445 binary itself (scratch worktree at f932c32, fresh release build, removed after) on THIS host TODAY reads 7.21/12.88/13.02 — the new build's numbers within noise, at a busier moment (1-min load ~13): the +4-6% is HOST-STATE drift, not code cost; loops 446-457 cost nothing measurable on the bench walk; tier ordering and Low-not-slower held (low p95 8.58 <= high p95 14.09 x 1.25). (4) CAPTURES: windowed_deck_low/mid/high.png re-rendered and INSPECTED — the same vista (POS 408.0/110.2/104.0 in every strip), lean at low (FPS 135), mid adds near-field settlement geometry (FPS 77), high the fuller roof field (FPS 74); the refreshed report carries the new date + the probe bullet. (5) REGRESSION: p3d workspace 690 green / 0 failed (14 suites); p3d-smoke OK (digest dd019eca900f5a61 UNCHANGED — headless, no bench path); p3d-people PASS (motion 0.66%, windowed p50 12.70 ms; four refreshed captures ride the commit, plaza/stride INSPECTED); idle-upgrade-check PASS; visual gates NOT run — nothing visual changed (the 445 bench-only precedent); root LOREFORGE workspace untouched (456 verified 480 green at this tree's crates). (6) PERF: this IS the perf job — baseline = 442/445's record, after = today's chain (same seed/scene/profile/host), the delta decomposed by the A/B and pinned to the host; no optimization claimed, none needed; the guard's own cost is six ~2.7 ms probes per chain (~16 ms), zero on any live path. LORE: canon touched: none — measurement infrastructure over the existing benchmark walk; no faction, place, event, term, NPC, item, or spell data; no identity assigned. World expression: unchanged — the Deck contract's numbers carry their own honesty seal. Migration: none (bench sidecar/report schema gained two columns + one bullet; no save format, no gameplay data; smoke chain unchanged). Deferred honestly: the contention guard is DONE; carried: the walk-snap query-bound observation; the is_water_layer/CTM-strip audit; guardian chronicle re-fire; multiplayer routing of terrain edits; geode pairing."
next_task: "Carry the list (p3d): the bench contention guard is DONE (458) and the quiet-host re-read is closed, so the next highest-value job per the priority ladder: (1) the walk-snap query-bound observation: surface.rs ground_at takes _from_y and DISCARDS it on the streamed path (the recovery's one-frame lift rides it) — write the pure law that makes the streamed placement explicit BEFORE the bound is ever enforced (unit law + route re-run, no behavior change), or (2) the is_water_layer/CTM-strip audit (447's note: the hard-coded 167 now points at dead_shrub's index — water proofs pass but the strip addressing deserves an audit). Carried deferrals: guardian chronicle re-fire; multiplayer routing of terrain edits; geode pairing (447's keepers are root-workspace lore)."
build: GREEN
tests: "p3d workspace 690 green / 0 failed (14 suites; pc3d_render 229 -> 233: the probe/band/report laws); make p3d-deck-bench PASS (low 7.18 / mid 12.92 / high 13.06 ms p50, probes 0.7% spread within the x1.4 band, A/B vs the f932c32 binary pins the +4-6% vs the 445 record to host drift); make p3d-smoke OK (digest dd019eca900f5a61 UNCHANGED); make p3d-people PASS (motion 0.66%, windowed p50 12.70 ms); idle-upgrade-check PASS"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/windowed_deck_high.png (the high tier's capture: the fuller settlement roof field under the same vista at FPS 74; its low twin shows the lean 135 fps foreground, mid the near-field geometry — tier dressing legible in pixels; docs/POORCRAFT-VALHEIM-STYLE-REBUILD/DECK-BENCH-REPORT.md carries the host CPU probe bullet)
blockers: "none; bench-only lib + app change (pc3d_render::deck + the --deck-bench arm) — no gameplay, save, worldgen, or authority code touched"
