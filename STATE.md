# STATE
loop_count: 445
current_milestone: THE BENCH TELLS THE TRUTH: make p3d-deck-bench's unquoted empty $(SEED) shifted argv, so every default run silently benchmarked the MID tier three times, dropped its captures into stray low/mid/high/report dirs (committed as debris in 444), and never assembled the report — fixed with a defaulted seed, the debris removed, and DECK-BENCH-REPORT.md + the three per-tier captures refreshed from a clean uncontended run (low 6.85 / mid 12.30 / high 12.56 ms p50)
last_done="The bench honesty fix (loop 445): a proof-integrity bug, not a feature. (1) THE BUG: the p3d-deck-bench target expanded $(SEED) empty and unquoted, the shell dropped the argument, and the binary's argv shifted — out_dir consumed the tier name (stray ./low ./mid ./high ./report capture dirs in the repo root; loop 444 committed their windowed_deck_mid.png debris) and the tier selector fell to the `_ => mid` arm, so all three 'tier' runs were mid runs onto one deck_bench_mid.csv and the 'report' invocation ran a FOURTH mid bench instead of calling deck_report. (2) THE RECORD, CORRECTED HONESTLY: DECK-BENCH-REPORT.md was last written by loop 442 — 443's 'DECK-BENCH-REPORT.md + bench PNGs refreshed' claim did not hold on disk (verified: no commit between 440 and 444 touched the report or shots/windowed_deck_*.png), and 443/444's quoted bench rows were mislabeled mid-tier numbers (444's contended low tier was documented as such at the time). No conclusion changes: the corrected per-tier numbers sit in the same noise band those loops claimed. (3) THE FIX: the target defaults the seed ($(if $(SEED),$(SEED),3) — the p3d-soak pattern); the debris directories are removed; a clean uncontended run refreshed DECK-BENCH-REPORT.md + the three per-tier captures: low 6.85 / mid 12.30 / high 12.56 ms p50 (p95 8.10/14.85/14.06; meshed 176/402/603, GPU 5140/18886/27587 KB) vs 442's 6.89/12.16/12.44 — noise-sized; the Low-not-slower contract law held. (4) EN-ROUTE RECONCILIATION: orientation found loop 444's air-steer work in the tree uncommitted; a concurrent session completed and pushed it mid-verification (f879fc3 + b3a5f23, github/main = b3a5f23). This loop's independent verification of that work AGREED: fresh make p3d-steer PASS x2 + comparator PASS (drift 1.56 m along the strafe, ortho 0.00 m; health 68% -> 39%; all four captures inspected), p3d workspace 657 green (pc3d_render 207), root workspace 476 green, p3d-smoke OK (digest dd019eca900f5a61 unchanged), p3d-assets OK. LORE: no canon data touched — tooling/proof integrity only; no migration. PERF: the fix changes no runtime code; the bench correction IS the perf claim (per-tier numbers above, same walk, same seed 3, same host, uncontended). Deferred honestly: a per-invocation contention guard for bench targets (detect + refuse under load) is unbuilt — this loop documented contamination and re-ran clean instead; the first bench attempt this loop ran while a concurrent root workspace test held the CPU (p50 21-65 ms) and was DISCARDED, not reported."
next_task: "Carry loop 444's list forward (unchanged by this fix): a walk-off route would prove the new commit LIVE (search the showcase seed for a deterministic >1 m ledge, stand at the edge, hold W — the body must fall, land, and take the impact law's damage; the unit law + wiring stand today); Space jump-off could be route-proven from a low hang (unit-lawed today); a look-pitch script hook would let the climb route frame the strand itself mid-climb; the lethal plaza-recovery branch still has no route proof. Deeper options: ExtendInventory/food loop depth, or a discovery audit."
build: GREEN
tests: "no Rust code changed — p3d workspace green (657; pc3d_render 207 incl. 6 air-steer/walk-off laws) and root cargo test --workspace green (476) stand on the same binary; make p3d-deck-bench PASS with per-tier rows (low 6.85 / mid 12.30 / high 12.56 ms p50) + report refreshed; p3d-smoke OK (digest dd019eca900f5a61); p3d-assets OK; make p3d-steer PASS x2 + comparator (independent verification of 444); idle-upgrade-check PASS"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/windowed_deck_mid.png (the refreshed mid-tier bench capture from the corrected target — the same contract walk the report's new mid row measures; no gameplay pixel changed, by design: this loop changed what the bench MEASURES, not what the game DRAWS)
blockers: "none; Makefile + evidence files only — no runtime code, no save/persistence change"
