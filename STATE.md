# STATE
loop_count: 410
current_milestone: NATURAL-WORLD REBUILD (owner pack docs/POORCRAFT-VALHEIM-STYLE-REBUILD, NWR-001..011): NWR-001..005 DONE (baseline guardrail; GLB asset factory; surface spike; streamed surface migration; caves/water/foundations); NWR-006 (materials/atmosphere) NEXT
last_done="NWR-005 — the surface path gained sparse caves, terrain-conforming water, and foundations. pc3d_render::world_features (new): CaveRegion grows a carved connected component from final_solid (the ONE authority) and meshes it by face-net extraction — quads exactly where solid faces carved, boundary faces from the same authority, so the cave welds to the surface with no duplicate faces or daylight leaks; ConformingWater draws one strip per river edge from the authoritative RiverGraph/FlowTable (width/speed from real discharge) whose 17 heights follow the surface patches + edit deltas, water_line = strip min - 0.45, banks where terrain rises, refresh_after_edit touching ONLY sections near the edited patches (the P3D-303 dirty law); check_foundation returns Valid{leveled_by}/Rejected{named reason} from walkable+slope+corner-level checks on live ground. PlayerBody now walks any CollisionSurface (new trait; SurfaceRegion implements it — walking the surface and feeling a raise underfoot is test-proven; the legacy authority path unchanged). Proofs: 5 unit laws + GPU cave_water_and_foundation_render_and_edit_locally (cave interior control-diff 0.63, 127 blue-dominant water pixels, an edit changes 1/5 sections — exactly the refreshed one, foundation Valid at +4 m) + windowed --play-caves / make p3d-caves (146 frames p50 0.43 ms): cave-interior capture human-inspected PASS (enclosing faceted stone, no sky leak, no seams), river before/after a 2x2 6 m raise dam with visible dam + ~1% image diff + LOCAL EDIT rows (4 dirty patches, 1/5 sections, refresh 9 us, water remesh 6 us). 9/9 visual gates green. REAL BUGS the proofs caught and fixed: a SurfaceRegion is a 48 m 3x3-PATCH window not a 3x3-region — build wrote 0.0 where refresh used the generator fallback, letting fake height changes hide (now both use the generator fallback and edit windows sit on the strip; the windowed proof caught it as an EMPTY dirty set); Raise STACKS on shared corner nodes (a 4x4 berm was a 24 m spike — the sky probe caught it); change-counting must compare heights OR the water line (an off-minimum raise moves heights but not the min). Previous: NWR-004 — ordinary natural terrain is now the SURFACE path: pc3d_render::surface_stream streams NWR-003 patches under the R3DV-006 laws (rings/bounded queue/per-frame caps/GPU budget/frustum) with ring LOD (17x17->9x9) and 2 m SKIRTS making LOD boundaries seam-safe without stitching; explicit TerrainPolicy keeps the legacy cube stream as fallback with all 9 gates green; collision served only from the full ring (refuses outside, within 1 m of the generator), edits feed collision. Proofs: 4 streamer tests + GPU vista (419 bounded frames, 1252 patches incl. the 1140-patch horizon ring) + windowed --play-surface-stream (251 frames p50 9.91 ms / avg 103 fps), human-inspected PASS — faceted hills to the skyline, no ring holes. Distance-gated eviction fixed the saturating-budget churn the teleport test caught. Previous: NWR-003 — the first real break from cube terrain, isolated to a 3x3 region: 17x17 boundary-grid patches with SEAM-SHARED edges (identical seam vertices test-asserted), authoritative heights/materials, sparse delta edits (Raise/Lower/Level) dirtying exactly patch+border-neighbors with a version table proving bounded remesh, collision from the SAME grid (bilinear height within facets, 3x3-stencil slope walkability that catches cliffs a cell-only check misses), compact delta save round-trip with corruption refusal. GPU+windowed proofs with cube-vs-surface discriminators (luminance bands, row-delta distribution vs banded flats, control-diff ground, edit-visible plateau); before-capture human-inspected PASS: faceted rolling natural hills, NOT cubes. A CW-winding bug that culling-invisible the terrain was caught by the pixel dump. Previous: NWR-002 — the renderer loads real glTF. THREE original assets ship as reproducible artifacts: prop.tree_ash (crooked trunk, 3 branch tiers, leaf clusters, roots — 1666/1800 tris), prop.rock_granite (fracture seam + moss — 98/600), module.house_croft (plinth/timber/thatch roof/chimney + 4 named sockets — 38/2500). Blender is NOT installed — the pack's alternative path: repository-owned Rust generator (tools/assetgen) writes valid glTF 2.0 GLBs deterministically (byte-identical regen). pc3d_assets::v2 validates the full schema-v2 + the factory-honesty law (non-planned status needs the .glb on disk; 15-case rejection matrix). pc3d_render::glb loads with named errors, LOD selection + coarsest fallback, sockets. Windowed proof --play-assets/make p3d-assets-window: 4 placements incl. two tree LODs in one frame, control-diff presence 0.22/0.33/0.34, foliage color check, 4 draws/1725 tris, p50 9.58 ms; PNG human-inspected PASS. The NWR-001 guardrail rows moved honestly (integrated + files on disk; inventory_is_truthful green). Previous: NWR-001 (over the completed R3DV-001..012 visual reset) — the rebuild baseline is secured: verification battery green (356/356 pc3d, 9/9 visual gates, smoke OK), the machine-readable capability_inventory.json records all 12 renderer paths honestly (module/representation/maturity/rebuild-target/proof; asset batch all planned, ZERO glb files on disk), and the new pc3d_render::inventory guardrail FAILS the suite on any false claim (sabotage-tested both directions — a coordinated pack+inventory shipped lie still fails on the missing disk file). Previous: R3DV-012 (the final task) — make p3d-visual-gates is the standing 9-gate regression battery: windowed-3d-axes, terrain-scenes, stream-walk, river-water, castle-city, npc-cast, quality-tiers, vertical-slice, asset-manifest — every windowed proof re-run with its semantic pixel assertions, loud failure, full console report to shots/gates_report.txt. Audit run 2026-09-07: 9/9 GATES PASS. VISUAL-GATES-REPORT.md holds the determination: per-gate frame records (all rendered gates >= 92 fps avg on the host iGPU, worst p95 21.6 ms; streaming steady-state ~1.1 ms/frame; tiers 42/80/102 patches at 2831/5468/6908 KB; showcase search 2.4 s), the human review (every gate PNG inspected in this audit and each task's original pass), and seven honest known limits. THE RESET CLOSED: from deterministic simulation + CLI PNGs to — windowed wgpu renderer, first-person walking with collision, terrain/caves/water/city/NPCs bound to the authoritative simulation, host-command building, quality tiers, save/reload, a walkable vertical slice on showcase seed 22, and a regression battery to keep it honest. Suites 356/356 pc3d + 474/474 root."
next_task: "NWR-006 (next per rebuild_roadmap.json): materials and atmosphere — the material library for surface/cave/water, texture LOD detail, atmosphere work; every visual change keeps make p3d-visual-gates green and cargo test green, determinations recorded on the .mds. The owner's manual play pass (make p3d-slice-live) remains the gate before the word 'playable'."
build: GREEN
tests: 474 passed / 0 failed (root workspace) + 356 passed / 0 failed (poorcraft3d workspace: 273 prior + 83 visual reset)
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/windowed_slice_showcase.png (showcase seed 22 — capital, water wheel, river, cast, one walkable world)
blockers: "none (the owner's manual play review is invited, not blocking)"
