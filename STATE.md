# STATE
loop_count: 304
current_milestone: P24-input-root-cause
last_done: "P24: input DEEPLY diagnosed and FIXED — root cause found with a synthetic-input harness (launch game, drive it with real macOS keystrokes via System Events, screencapture + trace-log every event): a stray `_ => {}` wildcard arm sat in the MIDDLE of the `match event` in window_event since P4, making every handler after it UNREACHABLE (Focused, KeyboardInput, MouseInput, MouseWheel — all dead code; the compiler only WARNED about unreachable_patterns and the warning drowned). That is why egui menus worked (different path) but every key and click was dead in every release; the game had only ever been verified by headless renders and smoke tests, never interactively played. Fix: wildcard removed, `#![deny(unreachable_patterns)]` added to lf_client so this class of bug can no longer compile silently. Empirically verified on the fixed build with synthetic input: E opens/closes inventory (ui_open=Inventory in trace), holding W walks (title-bar pos moved (1.2,-0.8)->(5.1,-4.9)), M toggles map, backquote opens console, typed 'fly'+Enter executed ([console] fly logged), Esc returns to play. Input instrumentation kept behind LOREFORGE_DEBUG_INPUT: per-event trace (event/ui_open/egui-consumed) + 1Hz tick summary (ui_open/locked/playing/keys_held/frame_ms) + F3 overlay now shows frame_ms. Planned InputRouter refactor skipped with evidence: the routing logic was always correct — the bug was dead-code suppression, now denied by lint; honest scope note in DEVLOG. 149 tests green; 20/20 vistest proofs; runtimes rebuilt"
next_task: "user playtest (this build is the first actually playable keyboard/mouse release); then backlog (sound, beds/doors, A*, PT-BR)"
build: GREEN
tests: 149 passing
last_screenshot: shots/vistest_console_preview.png
blockers: "github push still blocked by PAT missing workflow scope"
