# STATE
loop_count: 448
current_milestone: THE CROWD YIELDS: the settlement's people stop ghosting through each other — the NWR-009 deferral "NPC-vs-NPC avoidance (sim's domain)" closes as a pure world law: no body may enter a cell another body stands on or has claimed this tick; a blocked walker yields (sidesteps around the blocker, or waits keeping its route), cast order is the only tie-break, and a lone body traces exactly the old trajectory
last_done="The crowd yields (loop 448, POORCRAFT 3D): NPC-vs-NPC avoidance, the last NWR-009 sim-domain deferral. (1) THE LAW (pc3d_world::npc::step_crowd, pure): each tick builds the held set (every body's current cell) and a reserved set (cells claimed this tick); brains plan then move in cast order — a walker whose next cell is held or reserved YIELDS: one sidestep around the blocker (perpendicular to the step, then back, first free walkable in-patch cell — nav.local_of made pub), else stands WAITING with path and leg intact (never a teleport, never a lost route); the sidestep relocates the body and re-paths next tick as Idle so a yield cell can never read as an arrival (Working site is only ever the declared site). step() split into plan() + advance_leg(phase) with step() exactly plan+advance (byte-equal lone behavior, pinned by law). (2) THE WIRING: npcs::advance (the one call site every harness, proof, and the live slice steps crowds through) now delegates to step_crowd — renderer.rs and app.rs untouched. (3) LAWS: crossing walkers never share a cell on any of 400 ticks AND both land their declared sites; head-on walkers yield, arrive, and the scenario replays bit-identically; the sealed crowd (all three sidestep cells are bodies) holds the walker standing, same cell/path/leg, 50 ticks; the lone-brain trajectory through step_crowd equals brain.step tick-for-tick; and through the RENDER-facing advance, a staged head-on pair never shares a cell and both arrive (the wiring cannot silently drop the law). (4) EVIDENCE: p3d workspace 663 green ON THE COMBINED TREE (loop 446's step law + 447's root job + mine — pc3d_render 209 incl. the new cast law); make p3d-people PASS (12 NPCs, 7 draws, stride frames differ 1.03% — motion survives the law, captures inspected: rigs at distinct cells); make p3d-playtest x2 + comparator PASS with digest UNCHANGED 05c46411869a857c and the chain line identical ('fell 8 m over the open journal (health 33%)') — the law engages only when bodies actually meet, and the playtest's routes never do; p3d-smoke OK (digest dd019eca900f5a61 unchanged); p3d-assets OK. (5) PERF: UNAVAILABLE, honestly — two deck-bench attempts today read 9.81/17.41/16.14 then 20.60/36.70/32.56 ms p50 vs 445's clean 6.85/12.30/12.56, both under measured concurrent load (a mid>high inversion — impossible for a µs-scale sim change — marks the environment), both DISCARDED per the 445/446/447 precedent (three loops in a row have discarded every bench today); DECK-BENCH-REPORT.md + the three deck PNGs were RESTORED to HEAD (445's clean record stands untouched); analytical bound: the law adds two ~92-entry set builds + per-brain lookups per crowd tick — microseconds against 12 ms frames, no new hot-path allocation beyond three small collections per tick. LORE: canon touched: none (unnamed settlement brains; no faction/place/event/item data). Locked facts preserved: all. World expression: Valdenmoor's settlements are working places with people — people who walk through each other break the place; the yield (wait or step aside) reads as presence and courtesy, and stays LOCAL (a body yields to the body in front of it — no omniscient pathing). Migration: none (in-memory per-tick sim; the sidestep reuses Intent::Idle; no persisted field, save format, or proof-schema change). Deferred honestly: no ROUTE frames two NPCs yielding (needs a main.rs route staged head-on pair — main.rs was contended by loop 446's in-flight work when this job started); the quiet-host deck-bench re-read is now queued by THREE loops (446/447/448); the bench contention guard."
next_task: "Carry loop 447's list (unchanged priorities, both p3d): (1) THE UP-STEP HALF of the streamed walk — the surface path still snaps any RISE so cliffs remain climbable by walking (446's discovery; needs its wall/step law + route proof); (2) THE PLAYER-FACING DIG VERB on 446's live EditSurface path (key/bindings/targeting/drop tables) — pairs with 447's geodes (dig into a sealed pocket -> the keeper wakes). Carried deferrals: a staged-yield CROWD ROUTE (the 448 law is unit-lawed + render-lawed but no windowed route frames two NPCs yielding — needs main.rs); Space jump-off from a hang (unit-lawed only); the look-pitch script hook; the lethal plaza-recovery branch route; the quiet-host deck-bench re-read (queued by 446/447/448) + the bench contention guard; route-harness contention-hardened capture windows; is_water_layer/CTM-strip audit; guardian chronicle re-fire."
build: GREEN
tests: "p3d workspace 663 green / 0 failed ON THE COMBINED TREE (loop 446's committed step law + loop 447's committed root job + this law; pc3d_render 209 = 208 + the cast-level yield law; pc3d_world 264 = 260 + 4 crowd laws); make p3d-people PASS (12 NPCs, 7 draws, motion law 1.03%); make p3d-playtest x2 identical + comparator PASS, digest UNCHANGED 05c46411869a857c; p3d-smoke OK (digest dd019eca900f5a61 unchanged); p3d-assets OK; two deck-bench attempts DISCARDED as contended (documented above; report+PNGs restored to 445's clean state); idle-upgrade-check PASS"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/windowed_people_guard.png (the crowd close-up under the yield law — articulated rigs at distinct cells, the guard among them; from this loop's green make p3d-people run)
blockers: "none; pure sim law — no save format, worldgen, or asset change"
