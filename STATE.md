# STATE
loop_count: 432
current_milestone: WT-008 IN CODE: the local data-extraction surface — the mod-pack validator (12 sample packs, 5 good/7 bad-fail-named), the safety laws asserted verbatim, windowless LIVE exports (worldgen 289-region sample / the cast's brains / a charged machine chain) with the 8 common fields, and the 12-row exporter surface manifest — LOCAL ONLY
last_done="WT-008 in code (loop 432): the data-extraction plugin lab is executable. (1) pc3d_assets::plugins: the four WT-008 contracts embed+parse; validate_mod_pack enforces the per-kind laws (building door+interior, npc talk+>=2 variants, machine output+named blocked state, ui keymap-not-baked, seed deterministic, asset materials declared, perf claims need before+after; NaN caught pre-parse); the safety test asserts the contract's forbidden list VERBATIM (public_listener, external_upload_by_default, secret_export, unrelated_file_scan, silent_save_mutation) + the read-only save policy. The contract's own 12 sample packs: 5 good PASS, 7 bad FAIL NAMED (fake house no door / fake npc no talk / fake forge no output / baked UI key text / NaN bounds / missing materials / perf claim no before-after). (2) --export-data / make p3d-export-data (LOCAL ONLY): windowless LIVE exports — worldgen_sample (289 regions: biome/elevation/temp/humidity from the authority), npc (the showcase cast's LIVE brains with names+needs), machine (a fresh wired+fuelled+ticked chain, charge 1350 milli); each stamped with the contract's 8 common fields (asserted in-arm); the surface manifest maps all 12 rows to their exporter commands. EVIDENCE: pc3d_assets 36/36 (+3); EXPORT DATA OK (local-only); suites green. DEFERRED: no loopback MCP server yet (CLI-first per the contract; the same names map when one lands), no CSV/OBJ debug dumps (allowed, not required), telemetry local-first optional riding the observatory bundles, no player-save reads at all. Previous: playtest matrix (431), playtest capstone (430)."
next_task: "WT-008 LANDED (loop 432). Next per the WT queue: WT-009 (300+ semantic asset expansion in code — per-slot variant instancing in the wild, the deferred slice that puts the 300 GLB variants in the world by slot hash) and the remaining docs-pack slices as they come up. THE OWNER PLAY PASS of the stamped DMG remains the standing gate — the full progression loop (seed preview -> houses -> talk -> chest pick -> ore harvest -> forge -> iron bars) is live and proven deterministic."
build: GREEN
tests: "pc3d_assets 36/36 (mod validator: 5 good pass / 7 bad fail named; safety laws verbatim), p3d-export-data OK (local-only; common-field law asserted), full p3d + root suites green"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/export/worldgen_sample.json (WT-008: 289 live region samples from the worldgen authority — one of the three windowless exports; the visual captures stand from the playtest runs)
blockers: "none; unrelated dirty render/app/asset-capture files were left untouched for their owning work"
