# STATE
loop_count: 446
current_milestone: THE WALK-OFF LIVES: the loop-444 walk-off commit — dead code on the live streamed path (the walk's unbounded Y-snap swallowed every drop the surface answered, landing walked-off bodies soft and damage-free) — fixed by THE STEP LAW (the walk holds within one 1.05 m step; deeper drops are refused to the commit + arc, now shared by BOTH walk paths), and proven live by route_walk_off: the floor dug out under the standing body (the law's own second trigger — natural ledges ramp to walkable slopes at 1 m node interpolation, BY CONTRACT), feet 54.49 -> 49.49 EXACT, 100% -> 67-68% wounded, FELL toast, x2 + comparator PASS
last_done="The walk-off lives (loop 446): a proof-discovered bug + fix + the deferred live route. (1) THE BUG (pinned by a failing unit probe BEFORE the fix): walk_on_speed's Y-snap accepted ANY ground answer and the streamed surface's ground_at ignores from_y — the frame a body crossed a ledge edge it was teleported to the bottom instantly, damage-free; is_unsupported never saw a gap; the 444 commit was unreachable where routes walk. (2) THE STEP LAW: the snap holds only within SUPPORT_GAP_M (1.05 m); deeper, the walk refuses and the slice's walk-off commit flips the same airborne arc a jump uses; SUPPORT_GAP_M + is_unsupported moved to player.rs (app re-exports) and the slice's airborne machinery (commit/hang/jump/arc) HOISTED out of the rebuild-only branch so the law holds on both walk paths; new unit law the_walk_holds_a_step_and_refuses_a_ledged_drop (pc3d_render 207 -> 208). (3) THE HONEST LEDGE FINDING: the streamed surface interpolates at 1 m nodes — even the gen's vertical 4 m terrace faces ramp into walkable slopes; no WALKED natural edge can make the per-frame gap, so the route uses the law's second trigger: a floor dug out. (4) THE DIG, LIVE: UiAction::EditSurface -> Renderer::surface_edit -> SurfaceStreamer::edit (the delta layer that already persists) + new remesh_now — collision AND the drawn ground answer the edit in the same frame; the player-dig-verb foundation. (5) THE ROUTE: make p3d-walkoff — grounded teleport onto a deterministic flat vine-free cell near the plaza, 60 frames settle, the dig lowers the cell 5 m, the body falls straight down with no keys: feet 54.49 -> 49.49 (the dug floor EXACTLY), health 100% -> 67-68% (the impact law's ~35% for 5 m minus regen), FELL toast; PASS x2 + comparator PASS; the three captures INSPECTED (standing full-health / mid-fall on the dug wall with THE GROUND GIVES WAY / landed with FELL — HEALTH 64%, bar ~2/3). The first run's FAIL was a verdict bug (the route camera pose reports the EYE, +1.7 m over the feet) — fixed, not law-weakened. (6) REGRESSION: playtest x2 chain UNCHANGED (house-door walk survives the step law; ends 'fell 8 m over the open journal (health 33%)'); vine climb PASS x2 (same strand (65,20), health 100%); air steer PASS x2 (drift 1.49-1.55 m, ortho 0.00); p3d-smoke OK (digest dd019eca900f5a61 UNCHANGED); p3d-wilderness PASS; p3d-assets OK; p3d workspace 658 green (pc3d_render 208); root workspace green (479). LORE: no canon data touched — body traversal physics only; the falls of the Age of Reckoning answer the hand (444) and the ground (446); no migration (in-memory runtime state + the surface delta layer that already saved)."
next_task: "Two candidate next jobs, in order: (1) THE UP-STEP HALF of the streamed walk — this loop's fix bounded the snap DOWN only; the surface path still snaps any RISE, so cliffs remain climbable by walking (pre-existing, unobserved because the showcase is flat; discovered while bounding the down half). It needs its own wall/step law (block or refuse the move when the destination ground sits more than ~1 m above the feet) + a route proof — the natural counterpart to the step law. (2) THE PLAYER-FACING DIG VERB on this loop's live edit path (key/bindings/targeting/drop tables), which would turn EditSurface from a proof hook into a survival tool. Carried deferrals: Space jump-off from a hang (unit-lawed only), the look-pitch script hook, the lethal plaza-recovery branch route, a contention guard for bench targets, and a quiet-host deck-bench re-read (this loop's two bench attempts ran under a concurrent session's load — low 16.24 / mid 27.92 / high 31.73 ms p50 vs 445's clean 6.85/12.30/12.56 — DISCARDED, report + captures restored untouched from 445)."
build: GREEN
tests: "p3d workspace green (658; pc3d_render 208 incl. the step law); root cargo test --workspace green (479 on the combined tree — a concurrent session's UNSTAGED root work was present, staged by nobody); make p3d-walkoff PASS x2 + comparator PASS; make p3d-playtest PASS x2 (chain unchanged); make p3d-climb PASS x2 (unchanged); make p3d-steer PASS x2; p3d-smoke OK (digest dd019eca900f5a61 unchanged); p3d-wilderness PASS; p3d-assets OK; idle-upgrade-check PASS"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/walkoff-a/route_walk_off/walk_landed.png (the landed capture: the body at the dug cell's floor, the FELL — HEALTH 64% toast over the ~2/3 health bar — the walk-off law's wound, live)
blockers: "none; no save/persistence format changed (the surface delta layer already persisted)"
