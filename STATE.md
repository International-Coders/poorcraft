# STATE
loop_count: 354
current_milestone: Complete — world identity + 64-seed diversity laboratory (nightly-beta N05)
last_done: "loop 354 seed identity (N05): canonical WorldIdentity (seed_u64 + generator_version + world_type + mod_fingerprint) in the new lf_worldgen::identity module — stamped to identity.dat BEFORE generation, restored on load with field-by-field legacy fallback (seed.dat/genver.dat/fp 0), an explicit VersionMismatch policy (a legacy save announces generator drift instead of silently regenerating), documented salted channels from the full 64-bit seed, and the seed-text rules moved in (numeric exact / stable word hash / empty rolls once) with lf_client::slots delegating. New lf_worldgen::seedlab laboratory: per-seed lattice metrics (order-independent height/biome hashes, stats, biome histogram, water/river/cave fractions using the REAL carve predicate exposed as WorldGen::is_cave, surface mix, nearest kingdom, spawn proxy) + Jensen–Shannon biome distance + a 64-seed corpus report with calibrated floors and a same-seed bit-identical control — measured on v6: 2016 pairs, height L1 mean .159/p05 .090 (floor .020), biome JS mean .324/p05 .214 (floor .025), PASS; determinism tests cover same-seed bit-identity incl. negative/±1M/±i32::MAX coords, generation-order independence (commutative fold), and a 12-seed reduced corpus in the suite. xtask seedlab + make seedlab write target/seedlab_report.json. Client: create_world stamps identity, load_world restores + warns on drift, multiplayer Welcome now ADOPTS the server seed (streamer restart on change — it was ignored before), mod fingerprints (mod recipes ^ ore hooks) fold into the identity, F3 shows it. 450 tests, smoke OK, vistest unchanged 103/103 (visual seed atlases are N06), runtimes rebuilt."
next_task: "N06 — macro terrain, climate, rivers, and spawn diversity (docs/NIGHTLY-BETA/10-OVERNIGHT-JOB-QUEUE.md): use the N05 lab metrics to repair real low-diversity causes, improve correlated macro shape and spawn-quality choice, and render the seed/spawn atlases (seed_atlas_8, seed_same_control pixel-identical, spawn_quality_8) with before/after lab measurements."
build: GREEN
tests: 450 passed / 0 failed (loop 354)
last_screenshot: shots/vistest_hud_contextual.png
blockers: "none"
