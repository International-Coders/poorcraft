# STATE
loop_count: 367
current_milestone: POORCRAFT 3D P3D-102 — patch store, atomic saves, refusal law on disk
last_done: "loop 367 P3D-102 (P3D-100): worlds persist now. New crate pc3d_save (deps pc3d_core+pc3d_world; tempfile only for tests) — the ONLY crate that touches saves3d/. Framing law: every file = 16B P3D header | payload length u64 LE | payload | FNV-1a-64 checksum, pinned byte-exact; unframe runs the P3D-002 version law FIRST (open_decision), then readability floor (header+length field; a readable-but-truncated frame refuses LengthMismatch{declared,actual} — the precise refusal — rather than generic TooShort), then declared-vs-actual length, then checksum. paths: saves3d/<world>/world.p3d + saves3d/<world>/patches/p<x>_<y>_<z>.patch (signed decimal, pure function of coord). store: save_patch/save_world_meta atomic via tmp+rename (crash leaves only an invisible .tmp; rename consumes it), load_patch/load_world_meta guarded end to end; WorldMeta{seed,name}; store proven to write only inside its world dir. THE LAW ITSELF GOT HARDER: pc3d_core::open_decision had a real panic on 4..16-byte inputs (magic guard certified too little for decode) — now an explicit HEADER_LEN floor with a loop test over every partial length; found by the framing tests, fixed before commit. Two more framing bugs caught by proofs: checksum bytes counted into payload region (LengthMismatch on clean opens) and the truncated-tail verdict (TooShort vs LengthMismatch — chose precise). pc3d_world now re-exports its coordinate types at the crate root. 52 pc3d tests green (+10, incl. 1 law-hardening regression test), p3d-smoke OK, root workspace 474 green. Contract at docs/POORCRAFT-3D/contracts/P3D-102.md."
next_task: "P3D-103 — procedural macro elevation, climate, biomes, and deterministic patch regeneration (docs/POORCRAFT-3D/16-IMPLEMENTATION-WORK-BREAKDOWN.md): the first world CONTENT — use SeedStreams(terrain) to generate macro elevation/climate fields per RegionCoord, biome assignment with hydrology-flavored constraints (D-016), and deterministic empty-patch regeneration; patch hashes prove same-seed sameness (the P3D-104 atlas proof follows). Fill the contract first. NOTE: BETA-FOUNDATION track (original game) stays parked at B04."
build: GREEN
tests: 474 passed / 0 failed (root workspace, loop 360) + 52 passed / 0 failed (poorcraft3d workspace, loop 367)
last_screenshot: shots/vistest_biome_transitions.png
blockers: "none"
