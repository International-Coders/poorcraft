# STATE
loop_count: 459
current_milestone: THE QUERY-BOUND LAW: the ground query's origin contract made explicit (authority window vs streamed placement, pinned by tests) before any bound is enforced — the recovery lift's unbounded probe is now a named, law-guarded idiom
last_done="The query-bound law (loop 459, POORCRAFT 3D): closed STATE's next_task item (1) — the walk-snap query-bound observation carried since loop 445. (1) THE LAW (pc3d_render::player): CollisionSurface::ground_at's from_y documented as THE QUERY-BOUND LAW — authority path: the three-cell search window, now the pure predicate in_authority_query_window (answers lie in (from_y - 3, floor(from_y) + 1]); streamed paths: bounds nothing BY CONTRACT — a streamed mesh holds one height per column, the answer is the column's PLACEMENT, origin-independent (above the origin the rise law judges, below it the snap band and walk-off law judge); a future streamed bound must adopt and change this law consciously. (2) THE NAMED CONSTANTS: UNBOUNDED_QUERY_Y replaces the three bare f32::MAX/4.0 literals (renderer ground_y_at, settlement + npcs anchor windows) with the doc that the unbounded probe is a STREAMED-path idiom — the authority window under a near-f32-max origin holds only sky and REFUSES it (pinned by law); QUERY_REACH_M names the walk's own feet+2.0 origin (snap + rise's three sample sites). (3) 3 NEW UNIT LAWS: the streamed placement answers every query origin on the real region surface (placement == mesh height at four origins incl. UNBOUNDED_QUERY_Y); the authority window binds on real columns + refuses the unbounded probe + the pure window's hand cases; the live streamer's collision answers every origin identically in the loaded full ring and refuses outside it at every origin. (4) NO BEHAVIOR CHANGE, re-proven: make p3d-recovery PASS x2 + comparator (p50 26.1/26.8 ms; the lift lands at the rim, wake-up EXACTLY 0.5 — bar pixel-measured 0.975 rim vs 0.471 final, byte-identical a/b) and make p3d-walkoff PASS x2 + comparator (p50 25.7/25.7 ms; unwounded air beat, FELL — HEALTH 64% fresh at the latch); all refreshed captures INSPECTED. (5) REGRESSION: p3d workspace 693 green / 0 failed (14 suites; pc3d_render 233 -> 236); p3d-smoke OK (digest dd019eca900f5a61 UNCHANGED); idle-upgrade-check PASS; visual gates NOT run — nothing visual changed (the 445/458 bench-only precedent); root LOREFORGE workspace untouched (456 verified 480 green at this tree's crates). (6) PERF: not applicable — pure functions and named constants; zero new work on any live path. LORE: canon touched: none — a collision-query contract law over the existing walk/streamed-surface physics; no faction, place, event, term, NPC, item, or spell data; no identity assigned. World expression: unchanged — the same ground answers every body in Valdenmoor. Migration: none (pure law + renames + tests; no save format, no proof schema; smoke chain unchanged). Deferred honestly: next in line is the is_water_layer/CTM-strip audit (447's note: the hard-coded 167 now points at dead_shrub's index — water proofs pass but the strip addressing deserves an audit); carried: guardian chronicle re-fire; multiplayer routing of terrain edits; geode pairing."
next_task: "Carry the list (p3d): the query-bound law is DONE (459), so the next highest-value job per the priority ladder: (1) the is_water_layer/CTM-strip audit (447's note: the hard-coded 167 now points at dead_shrub's index — water proofs pass but the strip addressing deserves an audit; write the named-constant/law cure the same way 459 named the query bound). (2) Carried deferrals: guardian chronicle re-fire; multiplayer routing of terrain edits; geode pairing (447's keepers are root-workspace lore)."
build: GREEN
tests: "p3d workspace 693 green / 0 failed (14 suites; pc3d_render 233 -> 236: the placement/window/streamer laws); make p3d-recovery PASS x2 + comparator (p50 26.1/26.8 ms); make p3d-walkoff PASS x2 + comparator (p50 25.7/25.7 ms); make p3d-smoke OK (digest dd019eca900f5a61 UNCHANGED); idle-upgrade-check PASS"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/recovery-a/route_plaza_recovery/recovery_final.png (the one-frame lift's destination: the body woken at the plaza's heart at EXACTLY half health — bar pixel-measured 0.471 vs the rim's 0.975; walkoff-a's walk_air/walk_landed carry the snap band's own beats)
blockers: "none; pure law + named constants + docs over existing values (pc3d_render::player + the two streamed impls + three call sites) — no gameplay, save, worldgen, or authority code touched"

