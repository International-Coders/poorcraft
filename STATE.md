# STATE
loop_count: 460
current_milestone: THE PASS-ROUTING LAW: the water render channel classifies by art identity, not atlas position — the loop-447 audit's two live misroutings fixed and pinned (water tops translucent again, dead shrubs back to cutout)
last_done="The pass-routing law (loop 460, root LOREFORGE): closed STATE's next_task item (1) — the is_water_layer/CTM-strip audit carried since loop 447. THE AUDIT CONCLUDED: the CTM strip addressing is sound (UV math, generator placement, the 47-tile table, and the filler slot all agree — now pinned by the strip-addressing law), but the WATER-PASS ROUTING had drifted: is_water_layer classified by atlas position (tex == 10 || tex == 167), 167 was water's loop-332 marker when markers were small ints, and the marker base's move to 4096 (because real art grew into the low band) left the literal pointing at dead_shrub's layer. TWO LIVE MISROUTINGS on World::mesh_column (the client's AND the vistest harness's mesh path): every water TOP face carried marker 4098 and routed to the OPAQUE pipeline (REPLACE blend, depth-write on) — water surfaces rendered fully opaque, the art's alpha-170 translucency lost on all connected tops; every dead-shrub face (167) routed to the WATER pipeline (alpha blend, depth-write off). Existing proofs passed because water/decoration checks assert presence and hue, not translucency. THE LAW (lf_voxel::meshing): a vertex rides the water pass iff its tex is WATER ART — the named WATER_BASE_LAYER (mirrors lf_assets layer_of(\"water\")) or water's own strip-slot marker derived from the same mirror table that STAMPS it (routing can never disagree with stamping); atlas position is never identity; world::WATER_TEX_LAYER derives from the mesher const; ctm_marker_for/ctm_tile_uvs are public and lf_assets pins the one-way mirror cross-crate. 6 NEW LAWS (root 480 -> 486; lf_voxel 49 -> 52, lf_assets 21 -> 24): the real 3x3 water mesh routes EVERY vertex incl. 36 marker tops (FAILS on the old predicate); the drift witnesses 165/166/167/168 + grass_top + stone refuse; marker space routes by slot (only slot 2; unassigned markers refuse); the whole-atlas sweep (every named layer routes water iff it IS water); the CTM mirror matches CTM_BLOCKS row-for-row; the strip-addressing law (UV rect == generator rect over all 8x47 slot-tile pairs). PLAYER-VISIBLE, measured and inspected: fresh same-seed before/after renders — water_flow pool distinct colors 750 -> 1926 over 11k samples, g-variance 142 -> 156, hard tile seams gone (INSPECTED at 1.5x); first_person_view + hud_preview ~50% pixel change = the lake going translucent with the submerged shoreline reading through, HUD itself unchanged (pairs INSPECTED); transparency_layers shows the pool floor through the water behind glass (INSPECTED); plants_cross unchanged where it matters, edge water translucent (INSPECTED). REGRESSION: root cargo test --workspace 486 green / 0 failed; FULL vistest battery 108 scenes [ok] / 0 FAIL; make smoke OK; idle-upgrade-check PASS; POORCRAFT 3D untouched (separate workspace, no root deps). (7) PERF: not applicable — a mesh-build-time predicate (one const compare + one small match vs two literal compares); zero render-frame cost change; no claim. LORE: canon touched: none — render-pass routing over existing water/foliage art; no faction, place, event, term, NPC, item, or spell data; no identity assigned. World expression: water regains its intended translucency — rivers, lakes, and pools show their beds; Valdenmoor's waters read deeper. Migration: none (no save format, no data schema, no proof schema change). Deferred honestly: the carried list — guardian chronicle re-fire (447: the Discovery re-fires if a geode re-settles after despawn, dragon precedent); multiplayer routing of terrain edits; geode pairing."
next_task: "Carry the list (root): (1) guardian chronicle re-fire — the loop-447 deferral: the chronicle Discovery fires when a geode's keeper first wakes, but a geode that re-settles after its guardian despawned should re-fire like the dragon precedent; write the law + a proof. (2) Multiplayer routing of terrain edits — the client edit path is live (EditSurface/set_block); route player edits through the authoritative server path with replay/validation. (3) Geode pairing (447's keeper: geodes are the Covenant's anima_crystal world source; pairing = a discoverable narrative/geometric link worth a proof)."
build: GREEN
tests: "root cargo test --workspace 486 green / 0 failed (lf_voxel 52, lf_assets 24; +6 new laws); FULL vistest battery 108 scenes [ok] / 0 FAIL; make smoke OK (headless logic + GUI liveness); make idle-upgrade-check PASS"
last_screenshot: shots/vistest_water_flow.png (the pool as one connected translucent sheet — submerged terrain reads through; the before evidence is /tmp/passlaw/before, the pixel measure 750 -> 1926 distinct colors in the surface region; first_person_view's lake shows its bed through the surface)
blockers: "none; a mesh-build-time predicate + named constants + docs (lf_voxel::meshing + lf_assets laws); no gameplay, save, worldgen, or authority code touched; the six windowed_wild_*.png dirties are NOT this loop's and remain unstaged"
