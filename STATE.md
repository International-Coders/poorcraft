# STATE
loop_count: 469
current_milestone: THE FURNACE IS THE SERVER'S FIRE: server-side smelting over real UDP (protocol v9) — a bar only the realm's ledger funded. While connected, a furnace's whole economy is gated requests: a DEPOSIT consumes the player's canonical LEDGER into that furnace's commitment account (the local slot fills only when the verdict grants); a WITHDRAWAL pays out of the commitments and grants the goods back to the ledger (a fuel item only while its seconds remain banked — a burned coal cannot be taken back out); a SMELT-DONE — one per smelt the local furnace sim completes — transforms one committed input plus SMELT_TIME of banked burn into one BACKED output bar of the realm's own smelt law (lf_game::smelting, the same table offline plays). A refusal (a short ledger, an unbacked commitment, no banked burn, a replayed id) moves nothing and answers the smelter ALONE. This closes the last player-facing consumption tier that fabricates its own output: the fire yields what the adventurer truly carried. En-route the pack claim was made true: it now includes the held cursor (goods in hand are carried) — without it a drift upload between the pick-up and the deposit would mis-size the ledger against the very goods being committed. Offline furnaces play byte-equal to the shipped law (the integrated host is the same authority in process).
last_done="The furnace is the server's fire — server-side smelting over real UDP (loop 469, root LOREFORGE): closed STATE's next_task item (2) — the last player-facing consumption tier that fabricates its own output (smelting), chosen over the windowed two-client route per next_task's own rule and the 465-468 authority-gap precedent. THE WIRE (lf_protocol, v9): SmeltRequest { req_id, op: SmeltOp } — Deposit/Withdraw (per furnace pos + slot + item + u32 count) and SmeltDone (per furnace pos + input); SmeltVerdict { req_id, granted, reason } — NO item delta, because the client applied nothing before it (deposits and withdrawals WAIT for the verdict, so the PackSync claim stays exact); PROTOCOL_VERSION 8 -> 9. THE SERVER (lf_server): THE FURNACE ACCOUNT — one commitment account per (player, furnace pos), inputs/fuels/outputs maps + a banked-burn f32; THE FURNACE-OP LAW (the shared pure fn smelt_op): a deposit funds commitments (fuel banks its seconds at commit; junk banks none, withdrawable, never burned), a withdrawal pays out of them (the unburned-item law: burn >= the item's seconds or the fire has burned into it), a smelt-done transforms one input + SMELT_TIME burn into one backed bar of smelt_result — every refusal moves nothing; THE BURN RECONCILIATION keeps committed fuel within the banked seconds (deterministic id-sorted spend of positive-burn kinds — a replayed session agrees); ledger pre-checks before the account op (a deposit must be payable, a withdrawal's grant must fit the ledger's own 36-slot law — the escrow trial idiom); the replay window (the craft pays-once law's smelting twin); Goodbye burns out the leaver's accounts. THE CLIENT (lf_client): the furnace tick is the ONE SmeltDone site (output-count before/after the tick is the completion detector; the in-flight reports bounded at 64; a refused report reverts that furnace's own delta — input returns, the unbacked bar leaves; at-most-once, never fabricated); ui.rs's furnace_slot is the ONE gate for all three slots (the click's own before/after diff is read as at most one directional move by the pure furnace_move_intent — a swap of two stacks is two intents and refuses; the click is then UNDONE — nothing settles until the verdict; ONE player-intent move in flight, the storage rows freeze with a hint while it settles); deposits/withdrawals WAIT for the verdict (the offline branch keeps the shipped click-is-the-move law); the resolver is the one applier (granted moves settle exactly; a furnace that vanished under the verdict never eats the goods; a refused deposit whose UI closed does NOT re-return goods close_ui already returned — THE PROOF-TRACED DUPLICATION BUG, fixed before running the battery). THE PACK CLAIM IS TRUE (net.rs): the snapshot now aggregates the pack slots PLUS the held cursor — goods in hand are carried; without it a drift upload between the pick-up and the deposit would mis-size the ledger against the very goods being committed (the claim-with-cursor keeps every furnace op claim-invisible and ledger-exact in both UDP orderings). 11 NEW LAWS (root 534 -> 545; lf_protocol 7 -> 8, lf_server 23 -> 30, lf_client 110 -> 113): the smelt round-trip codec law; the furnace-account unit law (every door: unfunded smelts refuse by name, junk fuel banks nothing, three smelts spend one coal, backed bars withdraw, a burned coal cannot return, zero-count refuses); the deterministic reconciliation law (a mixed fire spends kinds id-first); the funded-deposit wire law (the ledger pays, the peer hears nothing, an unpaid deposit refuses); the backed-bar wire law (the full loop: 3 ore + fuel in, exactly 3 bars out, a fourth refuses, the ledger exact through the trade gate); the pay-once replay wire law; the two-fires wire law (commitments never pool); the burn-out wire law (Goodbye drops the accounts); the carried-hand law (the claim includes the cursor; committing the hand is claim-invisible); the furnace-move-intent unit law (one move per click, swaps refuse); the furnace source law (the tick is the one SmeltDone site, the verdict arm the one resolver, the slot gate the one move sender, all guarded on a live session). REGRESSION: cargo test --workspace 545 green / 0 failed (= loop 468's 534 + 11); make smoke OK (twice — logic + GUI liveness on the final binary); FULL vistest battery 110 scenes exit-0, every committed PNG byte-identical (md5 462b2318ed4c98d9a882266cd81fbd7d before == after) — singleplayer render paths pixel-proven unchanged; --features steam lib + examples compile clean with the v9 wire; runtimes refreshed (dist/ dmg + linux tarball + .app + server); Windows exe honestly skipped (mingw absent); POORCRAFT 3D untouched; the six windowed_wild_*.png dirties remain deliberately NOT staged. PERF: not applicable — one small request datagram per furnace intent and per completed smelt (one per 10 s of burning), one small verdict back; the server adds one bounded map update per op; zero singleplayer cost (offline branches unchanged). LORE: canon touched: none — multiplayer economy plumbing (the 465/466/467/468 precedent); the furnace law is the existing smelt/fuel tables; no faction, place, event, term, NPC, item, or spell data changed; no new canon text (rejection reasons are UI hints). Canon preserved: the chronicle as the player-authored history; Anima as a material energetic property (a furnace is fuel and fire, not a miracle — every bar is paid from ore and burn the realm knows); no identity assigned. World expression: in shared Valdenmoor the forge gives what the realm's ledger can pay for — ore committed to a fire is ore the adventurer truly carried, a burned fuel is spent, and every bar the fire yields is the realm's own accounting. Migration: PROTOCOL_VERSION bump only (matched client+server; old peers rejected by the existing gate) — no save format, no block/item change, GENERATOR_VERSION unchanged, ClientSave untouched (furnace commitment accounts are session-local server memory)."
next_task: "Carry the list (root): (1) The windowed two-client route — a second client SEES a dig land (the GPU-side end of the wire laws): GameState owns a real window+wgpu surface, so the honest route needs a headless client driver (drive GameState-free logic or a surfaceless render path) — scope that driver first, then the route: server + client A digs + client B renders the hole, pixel-gated. (2) Eating — the last client-side consumption tier of the same shape (smelting DONE loop 469): server-side eating gates food against the LEDGER the same way crafting/smelting/placement do; after it, every player-facing consumption of the played loop is server-computed. Check STATE/build first: prefer the higher-severity item per the priority ladder."
build: GREEN
tests: "cargo test --workspace 545 green / 0 failed (= loop 468's 534 + 11 new laws: lf_protocol 7 -> 8, lf_server 23 -> 30, lf_client 110 -> 113); make smoke OK (logic + GUI liveness); vistest battery 110 scenes exit-0, all committed scene PNGs byte-identical (md5 462b2318ed4c98d9a882266cd81fbd7d)"
last_screenshot: "none new (nothing visual changed — the 462/464-468 precedent; the furnace UI's pixels are identical, only its online moves are gated); the newest visual scene remains shots/vistest_resonance_compass_hud.png (loop 464)"
blockers: "none; the six windowed_wild_*.png dirties are NOT this loop's and remain unstaged"
