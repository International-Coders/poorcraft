# STATE
loop_count: 431
current_milestone: THE PLAYTEST MATRIX COMPLETE: chest (loot: the first pick) + ore harvest (needs the pick) + map marker read, and the CLOSED ORE LOOP (the forge consumes harvested stock; bars land in inventory) — the semantic playtest now covers EVERY matrix row in one deterministic walk
last_done="Playtest matrix complete (loop 431): the three remaining rows live and chained. (1) THE PROPS: prop.chest + prop.ore_node placed at deterministic plaza offsets on real ground, recorded as plaza interactables (2.2m zones winning the E-resolver before forge/NPC). (2) THE ROWS: open_chest (one loot drop: WOOD PICK + bread, then honestly empty), harvest_resource (2 iron_ore per swing, REQUIRES the pick — bare hands yield nothing, the journey's own gate law; the node has 6), open_map_marker (the plaza banner reads); a generic interact panel + the stock-lines view. (3) THE CLOSED ORE LOOP: items.rs +iron_ore(6)/iron_bar(7); SliceHost carries a real items::Inventory (12 slots) + ore yield + chest flag; the forge's H CONSUMES harvested stock (never granted; honest refusals named); taken bars land in the inventory. EVIDENCE: the extended route_semantic_playtest — house entered (frames differ), Maren Oldford talked, 1 bar forged, 4 ore harvested, chest+marker read, IRON BAR in stock — 8 captures p50 10.4ms, EVERY required_steps row covered; make p3d-playtest x2 identical + comparator PASS; suites green. BUGS THE PROOF CAUGHT: the closed loop re-ordered the route itself (first run forged 0 bars — harvest BEFORE the load) and an out-of-order shot schedule silently skipped four captures. DEFERRED: no collision/animation on the props, static marker text, bread carried but not eaten in-slice. Previous: playtest capstone (430), house entry (429)."
next_task: "PLAYTEST MATRIX COMPLETE (loop 431). Next per the WT queue: WT-008 data extraction in code (local exporter surface + telemetry files over the existing evidence bundles), then the remaining docs-pack implementations as their slices come up. THE OWNER PLAY PASS of the stamped DMG remains the standing gate — the game now has the full progression loop: seed preview -> enterable houses -> talkable villagers -> chest pick -> ore harvest -> working forge -> IRON BARS."
build: GREEN
tests: "p3d-playtest x2: identical full-matrix walks + comparator PASS; every required_steps row covered; full p3d + root suites green"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/playtest-a/route_semantic_playtest/play_harvest.png (mid-walk: the pick from the chest swinging at the ore node — the run ends with IRON BAR in stock)
blockers: "none; unrelated dirty render/app/asset-capture files were left untouched for their owning work"
