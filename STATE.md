# STATE
loop_count: 357
current_milestone: Stage A started — B01 runtime truth dashboard (beta-foundation)
last_done: "loop 357 B01 runtime truth dashboard (Stage A of docs/BETA-FOUNDATION/08-BETA-DELIVERY-ROADMAP.md): the engine's honest inventory is now machine-readable and contract-tested. New xtask/src/truth.rs tracks 21 systems across four ownership classes (ServerAuthoritative / RelayOnly / ClientLocal / DeterministicGenerator) with per-row evidence paths and — for server-authority claims — marker strings that must appear in the LIVE lf_server source (include_str!, compile-time-anchored). The B01 contract test enforces both directions: none of the 13 audited client-only systems (survival, inventory/crafting, fluids, machines, mobs, NPCs, settlements, companions, quests, reputation, research, saves, Steam transport) may be relabeled authoritative without an explicit audit-list change AND a real server implementation whose markers exist today; and if the server ever loses session/SetBlock handling, the dashboard fails instead of quietly overclaiming. Server authority truthfully = session_and_peers + block_edits, nothing else. `xtask truth` (make truth) writes target/truth_report.json — versions (protocol v4, generator v7), counts (107 scenes, 461 #[test] attrs / 84 files), ownership summary, optional live perf via --bench, seedlab fold-through — and build_report validates evidence paths + audit-list coverage at runtime too. Also committed the docs/BETA-FOUNDATION/ goal pack and the MASTER-PLAN deprecation pointer. 459 tests (+6), smoke OK. Tooling-only job: dist binaries unchanged, runtimes not rebuilt."
next_task: "B02 — fixed tick, command IDs, and domain events (docs/BETA-FOUNDATION/08-BETA-DELIVERY-ROADMAP.md Stage A): introduce deterministic tick/order primitives around existing behavior with snapshot-hash tests; no visible redesign. Done when render cadence and command batching do not alter a representative simulation result."
build: GREEN
tests: 459 passed / 0 failed (loop 357)
last_screenshot: shots/vistest_biome_transitions.png
blockers: "none"
