# STATE
loop_count: 444
current_milestone: THE AIR STEER: the fall answers the hand — a falling body keeps reduced lateral authority (a fixed 0.45 fraction of the walk, sprint never applies mid-air), walking off a ledge now COMMITS the fall instead of hover-gliding, and the whole verb is proven live by the new route_air_steer (the same 5 m drop twice: the free fall holds its line exactly, A held drifts it 1.5 m along the strafe axis, both drops land the same wound)
last_done="The air steer (loop 444): closed the standing 441/443 deferral 'mid-air steering is now routable but still untested as a law'. (1) THE LAWS (pc3d_render 201 -> 207, pure in app.rs): lateral_speed (the speed selector — full walk or sprint on the ground, WALK_SPEED * AIR_STEER_FRACTION = 1.8 m/s in the air regardless of sprint), is_unsupported (the walk-off law: no surface holds a body whose ground answer sits more than SUPPORT_GAP_M = 1.05 m below the feet) + six test laws: the selector matrix, a falling body drifts one steer-speed per second while an unsteered fall holds its line EXACTLY, the air drift is the walk scaled by the fraction (ratio measured 0.45 +- 0.01), the same drift at 30/60/120 fps, steering into the strand catches what the free fall misses (composition with the grab law — the steered hand reaches the line as the span opens), and the walk-off commits past one step and not before. (2) THE WIRING: the frame block feeds the walk lateral_speed(slice.falling, sprinting), gates wants_sprint on !slice.falling (no stamina burn mid-air), and — the real bug found en route — commits slice.falling with vy 0 when a GROUNDED body's support vanishes: the old walk kept hover-gliding at full walk speed over any drop deeper than its step window, gravity never engaged; the commit uses the SAME ground_y_at answer the arc lands on, so commit and landing can never disagree (excluded while hanging — the strand owns the body). (3) THE ROUTE: route_air_steer drops the player twice from 5 m over the plaza banner (Plains grows no vines BY DESIGN — no strand can catch either drop): the FREE fall's mid-air pose is recorded through the camera (= the body) and equals the drop XZ to < 0.05 m; the STEERED fall (A held from teleport through the fall via key_script, released before the 60 fps landing so no ground walk pollutes) drifts along = 1.50 m in the strafe axis (bound 0.35-2.6) with ortho 0.00 m; both drops land real wounds (health 68% -> 38%, the two 5 m impacts + regen; the bands exclude a plaza recovery's exact 0.5); captures steer_free_air/steer_free_landed/steer_held_air/steer_held_landed pixel-separated and INSPECTED (the birch trunk entering the held mid-air frame IS the drift). make p3d-steer PASS x2 identical + comparator PASS. (4) REGRESSION: playtest x2 digests UNCHANGED (05c46411869a857c — the wiring never perturbs the no-input chain); vine climb PASS x2 unchanged (same strand (65,20), health 100% — the tip-release fall steers nothing without keys); p3d-smoke OK (digest dd019eca900f5a61); p3d-wilderness PASS (6 captures); p3d-assets OK. PERF: deck bench mid 12.39 / high 12.36 ms p50 vs 443's 12.30-12.38 — noise; the bench attaches NO slice (verified in the bench cfg — camera_script + frame_hooks only), so the walk change has no bench cost path; the low tier read 12.17 vs 442's 6.89 under a concurrent 100%-CPU test process from another session (load 3.4-6) — contended, not comparable, documented. EVIDENCE: p3d workspace green (pc3d_render 207, workspace exit 0); root cargo test --workspace green (476); idle-upgrade-check PASS. LORE: no canon data touched — body traversal physics only; the falls of the Age of Reckoning answer the hand (overgrown canopies and cliffs become deliberate traversal, not scripted set pieces); no migration (in-memory runtime state, pure per-frame physics). Deferred honestly: the walk-off commit is live-wired + unit-lawed but has no dedicated route (needs a deterministic >1 m ledge walk-off; the plaza is flat by design); Space jump-off from a hang is still unit-lawed only; the look-pitch script hook for the climb framing is still open; the lethal plaza-recovery branch still has no route proof; THE OWNER PLAY PASS of the stamped DMG remains THE standing gate."
next_task: "THE AIR STEER SHIPPED (loop 444). Natural follow-ups: a walk-off route would prove the new commit LIVE (search the showcase seed for a deterministic >1 m ledge, stand at the edge, hold W — the body must fall, land, and take the impact law's damage; the unit law + wiring stand today); Space jump-off could be route-proven from a low hang (unit-lawed today); a look-pitch script hook would let the climb route frame the strand itself mid-climb; the lethal plaza-recovery branch still has no route proof. Deeper options: ExtendInventory/food loop depth, or a discovery audit."
build: GREEN
tests: "p3d workspace green (pc3d_render 207 incl. 6 new air-steer/walk-off laws; workspace exit 0); make p3d-steer PASS x2 identical + comparator PASS (drift 1.50 m along, ortho 0.00); make p3d-playtest PASS with digests UNCHANGED (05c46411869a857c); make p3d-climb PASS x2 (unchanged, health 100%); make p3d-wilderness PASS (6 captures); p3d-smoke OK (digest dd019eca900f5a61 unchanged); p3d-assets OK; deck bench mid 12.39 / high 12.36 ms p50 (noise; low contended by a concurrent foreign process, documented); root cargo test --workspace green (476); idle-upgrade-check PASS"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/steer-a/route_air_steer/steer_held_landed.png (grounded beside the birch the drift rounded — the marker boxes sit to the right where the unsteered line would land, the toast 'FELL — HEALTH 34%' across the HUD, the health bar at ~1/3: the steered fall landed its wound 1.5 m from where the free fall landed its own)
blockers: "none; the steer + walk-off commit are in-memory runtime state on the slice — no save/persistence change, no collision change (the walk-off commit only flips the SAME airborne state a jump already uses)"
