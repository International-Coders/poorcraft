# STATE
loop_count: 429
current_milestone: HOUSE ENTRY LANDED (WT-002 slice 5, the last honest unavailability): buildings enterable (ring solid, interior + plaza-facing door open), live settlement collision mounted over the streamed surface, route_house_entry UNAVAILABLE->PASS — 8/8 required observatory routes green, zero unavailabilities remain
last_done="House entry (loop 429): town buildings are enterable. (1) THE REFINEMENT (assemble_kit, mirroring the gate): every plan building's interior cells leave the collision set (the wall RING keeps them), the door column — the ring cell at the center of the plaza-facing edge — opens, and DoorEntry {kind, door, interior} records the way in. (2) THE LIVE COLLISION: attach_settlement keeps cells + doors; walk_player_surface_speed consults SettlementGround over the streamed surface (walls stop the player, doors + interiors pass); CollisionSurface gained a blanket ref-impl so adapters compose over borrows. (3) THE PROOF HOOK: UiAction::PlayerTeleport (the PlayerLook precedent) moves the PLAYER grounded through the live surface — the first route attempt set the renderer camera directly and barely differed because the slice loop re-poses the camera from the player every frame (the mouse-fix lesson, found again by the proof). (4) THE ROUTE: teleport outside the door -> capture; teleport to the interior -> capture; assert >15% difference. EVIDENCE: route_house_entry PASS — 56.4% difference (p50 ~15ms); make p3d-observe 8/8 required routes PASS, ZERO unavailabilities; the WALK LAW: ring-beside-door solid, door open, interior open, 400-step walk in through the door, parallel walk at the ring row STOPPED; ui-shots 13/13; suites green. DEFERRED: teleport is proof-only (not a gameplay ability), no interior props yet (the anchor cells exist), no door animation, cardinal door facing. Previous: forge (428), NPC talk (427), markers (426). The full interaction trio (talk + forge + entry) is live; the owner play-pass gate stands.""
next_task: "HOUSE ENTRY LANDED (loop 429) — ALL 8 required observatory routes PASS, zero unavailabilities. Next per the WT queue: the SEMANTIC PLAYTEST ROUTE (WT-002 slice 5 capstone) — one scripted walk chaining talk + forge + house entry in a single world (the observatory already proves each; the playtest chains them), then WT-008 data extraction in code and the remaining WT docs-pack implementations. Rebuild + re-verify the stamped DMG for the owner play pass (the game now has seed preview, talk, forge, and enterable houses)."
build: GREEN
tests: "--observe 8/8 required routes PASS (house_entry 56.4% outside-vs-inside, npc_talk speaking, forge_use 1 bar), the enterable-house walk law, ui-shots 13/13 regression, full p3d + root suites green"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/observatory/route_house_entry/beauty_interior.png (standing INSIDE a town home through the open door column — walls around, the door way out)
blockers: "none; unrelated dirty render/app/asset-capture files were left untouched for their owning work"
