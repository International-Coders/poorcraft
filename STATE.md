# STATE
loop_count: 305
current_milestone: P25-correctness-honesty-sweep
last_done: "P25 correctness & honesty sweep: server SetBlock now validates against the real block registry (mod ids >= 100 accepted; the old `block <= 18` cap silently dropped every mod edit in multiplayer) and the dedicated server loads mods/ at boot like the client; lf_steam's `steam` feature actually compiles now (steamworks 0.12 optional dep, feature-off default untouched, test made honest about a running Steam client); worlds stamp a generator version (`genver.dat`, mismatch warns because revisited unedited chunks regenerate); lantern block got a real 42nd atlas layer (was falling through to stone); root Cargo.toml dependency table pruned to the 21 deps actually used (the old table listed 14 fiction deps with drifted versions); mods/README `_ore` auto-registration is real code now; stale BACKLOG entries (P6 mobs/combat, P3 sky) corrected; misleading tests/golden stub removed. NEW: vistest renders are pixel-analyzed after saving (lf_vistest::verify_render: non-uniform luma + >= 16 distinct colors) — the gate immediately exposed that BOTH raytraced proof scenes (and in-game Live RT / R-key captures, since P18) rendered one flat color due to two pathtracer bugs: WGSL DDA t_max initialized with a signed numerator (negative for negative ray components -> rays marched one axis into the void) and a double to_radians() on the already-radians fovy (camera basis at ~1.4% -> all rays parallel). Both fixed; RT proofs show real terrain with soft shadows and emissive night glow again (1697 / 294 distinct colors). Server UDP tests hardened with a deadline-based drain_until (fixed sleeps were flaky under parallel chunk-gen load). 154 tests green; 20/20 vistest proofs pass the pixel gate; smoke OK; server boot loads 2 mods"
next_task: "visual-identity milestone (per-face material mapping incl. proper grass top/side/bottom, alpha-cutout leaves + wind, smooth AO, crack overlay + mining particles, mipmaps) — approved direction: hybrid-selective art (connected projection on large man-made materials later), Deck-60fps floor, all four platforms launch-blocking"
build: GREEN
tests: 154 passing
last_screenshot: shots/vistest_raytraced_shadows.png
blockers: "github push still blocked by PAT missing workflow scope"
