# STATE
loop_count: 449
current_milestone: THE WALL HOLDS: the up-step half of the walk law — the streamed surface's ground snap accepted ANY rise, and the streamed path's cell_solid answers false everywhere, so every cliff face and dug wall was a free elevator (fall into a pit, walk straight out); the walk may now raise the feet only onto a walkable surface — a discrete step within one step height or a ramp within the nav's own MAX_WALK_SLOPE contract, measured as a SIGNED 1 m-baseline slope along the move so the verdict is geometry, not the frame
last_done="The wall holds (loop 449, POORCRAFT 3D): the up-step half of 446's step law, closing STATE's next_task item (1). (1) THE LAW (pc3d_render::player::rise_accepted + PlayerBody::rise_refused_on, pure): the streamed surface's CollisionSurface answers cell_solid=false everywhere ('slopes gate walkability through ground_at stepping'), so the walk's ground snap was the only wall — and its condition `pos[1] - g <= SUPPORT_GAP_M` is trivially true for any rise, so walk_on_speed teleported the body to the top of ANY cliff or dug wall. The rise law now refuses an axis move whose target ground sits above the feet unless the surface there is itself walkable: a discrete step within SUPPORT_GAP_M (the up twin of the down law) or a ramp within the nav's own MAX_WALK_SLOPE (surface.rs), measured as a SIGNED rise along the move over a 1 m baseline (the mesh's own node spacing) — geometry, not per-frame rise, so the verdict is the same at 30/60/120 fps; a descent ahead is never a wall (the down law owns descents). (2) THE PROBE'S BUG, caught live and promoted to a law: the first draft measured |ahead-behind|, so the steep ramp BEHIND a body poisoned the baseline and glued it to the wall it had just been refused by (and fps jitter reshuffled the samples and unstuck it — the pit-wall route's W-hold sat motionless 60 frames, then lurched); the route harness's key_script/pose records pinned it, the signed slope fixed it, and the_body_walks_away_from_a_wall_it_was_refused_by pins it forever. (3) THE HOOK: UiAction::PlayerFace {yaw, pitch} — the body's ABSOLUTE facing through exec_actions (also serves the carried look-pitch deferral for route framing). (4) THE ROUTE (make p3d-pitwall, route_pit_wall): the walk-off's own flat dig cell is dug 5 m (the body falls — the walk-off law's sequence), the cell TWO toward -z is dug 4.5 m (the BETWEEN cell becomes a gentle ramp; a one-cell-away dig ACCUMULATES on the shared border nodes and tilts the pit deeper — the first staging attempt made the body fall twice, and the strip validation in find_dig_spot_pair keeps the step a step); the body holds W into the pit's 5 m wall — REFUSED at the base — then faces the step (pitch down), walks the ramp onto the step cell, and is held again by the step cell's own outer wall; feet 49.49 -> 49.49 -> 49.87 (the live step floor), health 100% -> 76%, FELL toast, x2 identical + comparator PASS. (5) EVIDENCE: p3d workspace 667 green / 0 failed (pc3d_render 213 = 209 + 4 rise laws); the four captures INSPECTED (in-pit wounded + FELL toast; held at the wall; on the step at the outer wall; final stable); make p3d-walkoff PASS x2 + comparator (UNCHANGED: same dig cell (391.5,132.5) — the shared find_dig_spot refactor kept the pick byte-equal — same fall 56.19 -> 51.19, 67%); make p3d-playtest x2 + comparator PASS, bundle digest UNCHANGED 05c46411869a857c (the plaza walk never meets a rise; the law engages only where bodies meet walls); make p3d-climb PASS x2 (same strand (65,20), landed unharmed 100%); make p3d-steer PASS x2 (drift 1.53/1.55 m, ortho 0.00 — the air keeps steering; a falling body's XZ now also stops at wall faces instead of clipping into them); p3d-smoke OK (digest dd019eca900f5a61 unchanged); p3d-assets OK; idle-upgrade-check PASS. (6) PERF: no claim, honestly — the law adds at most three ground_at samples (HashMap lookup + bilinear) per moving axis per frame, allocation-free, microseconds against 12 ms frames; the host is still contended (a foreign 98.6% CPU process, 15-min load 8.2), so no bench attempt was recorded (the quiet-host re-read stays queued, now by four loops). LORE: canon touched: none — body traversal physics only. Locked facts preserved: all. World expression: labor in Valdenmoor has consequence — the pit you dig is a pit until you dig steps or ramps out; cliff faces of the overgrown wilds are walls to route around, not free elevators; the fall economy of 441/444/446 becomes coherent (you can fall in; you cannot walk out). Migration: none (pure in-memory walk predicate; no persisted field, save format, or proof-schema change). Deferred honestly: the player-facing DIG VERB (next_task item 2 — now pairs with BOTH 447's geodes and this law: a dug pit HOLDS, so stairs/ramps are a real player skill); the staged-yield CROWD ROUTE (main.rs is free as of this loop); the quiet-host deck-bench re-read + contention guard; Space jump-off from a hang; the lethal plaza-recovery branch route; route-harness contention-hardened capture windows; is_water_layer/CTM-strip audit; guardian chronicle re-fire."
next_task: "Carry the list (p3d): (1) THE PLAYER-FACING DIG VERB on 446's live EditSurface path (key/bindings/targeting/drop tables) — pairs with 447's geodes (dig into a sealed pocket -> the keeper wakes) AND 449's wall law (a dug pit HOLDS the body; digging stairs/ramps to climb out is now a real player skill — EditSurface already remeshes the same frame). (2) The staged-yield CROWD ROUTE (the 448 law is unit-lawed + render-lawed but no windowed route frames two NPCs yielding — main.rs is now uncontended). Carried deferrals: Space jump-off from a hang (unit-lawed only); the lethal plaza-recovery branch route; the quiet-host deck-bench re-read (queued by 446/447/448/449) + the bench contention guard; route-harness contention-hardened capture windows; is_water_layer/CTM-strip audit; guardian chronicle re-fire."
build: GREEN
tests: "p3d workspace 667 green / 0 failed (pc3d_render 213 = 209 + 4 rise laws: a_step_up_is_walked_and_a_wall_refuses_the_move, the_rise_verdict_is_the_slope_not_the_frame, the_dug_pit_holds_the_body_until_the_step, the_body_walks_away_from_a_wall_it_was_refused_by); make p3d-pitwall PASS x2 + comparator PASS (captures inspected); make p3d-walkoff PASS x2 + comparator (unchanged — same cell, same fall, 67%); make p3d-playtest x2 + comparator PASS, digest UNCHANGED 05c46411869a857c; make p3d-climb PASS x2 (unchanged); make p3d-steer PASS x2 (unchanged); p3d-smoke OK (digest dd019eca900f5a61 unchanged); p3d-assets OK; idle-upgrade-check PASS; no bench recorded (host contended — documented above)"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/pitwall-a/route_pit_wall/pit_step.png (the body ON the dug step inside the 5 m pit, held by the step cell's own outer wall — from this loop's green make p3d-pitwall run)
blockers: "none; pure walk law — no save format, worldgen, or asset change"
