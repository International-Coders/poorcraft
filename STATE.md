# STATE
loop_count: 364
current_milestone: POORCRAFT 3D P3D-004 — profiler counters + baseline record
last_done: "loop 364 P3D-004 (P3D-000 stage): the engine can measure itself from birth. pc3d_core::profile: CounterId vocabulary fixed in enum order (mesh_work/fluid_work/path_requests/entity_ticks/network_bytes/save_bytes/patch_rebuilds/journal_events — the 03-ENGINE performance-principles list) with saturating inc/add and enum-ordered snapshots (never map order); FrameTimes fixed-cap ring with nearest-rank percentiles verified on a known 1..100 sample (p50=50, p95=95, ring-wrap drops oldest, invalid NaN/negative samples dropped, arrival-order bit digest); MemoryCounters explicit-call with net_bytes arithmetic; BaselineRecord with a hand-rolled key-ordered deterministic JSON writer (escapes tested). poorcraft3d --baseline runs a deterministic synthetic workload (600 frames of stream-jittered frame times through the real FixedClock) and prints the record — two consecutive runs are byte-identical (same sha256). Measured baseline: 600 frames, p50 22.4ms p95 31.8ms min 11.0 max 32.9, 805 entity ticks (600 frames ~13.4 sim-seconds x 60Hz — the clock tracked exactly), mesh_work 1800, journal_events 600. 28 pc3d_core tests green (+4); root untouched at 474. Contract at docs/POORCRAFT-3D/contracts/P3D-004.md."
next_task: "P3D-005 — first empty-world runtime and a headless smoke path (docs/POORCRAFT-3D/16-IMPLEMENTATION-WORK-BREAKDOWN.md): the P3D-000 stage capstone — a runnable loop that advances the FixedClock, ticks a minimal world stub, records frame times and counters for real, and a make-p3d-smoke target that runs it headless and asserts liveness + determinism. Renderer/window can wait; the RUNTIME is the deliverable. NOTE: BETA-FOUNDATION track (original game) stays parked at B04."
build: GREEN
tests: 474 passed / 0 failed (root workspace, loop 360) + 28 passed / 0 failed (poorcraft3d workspace, loop 364)
last_screenshot: shots/vistest_biome_transitions.png
blockers: "none"
