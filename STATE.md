# STATE
loop_count: 456
current_milestone: THE CAPTURES READ THE LATCH: the walk-off's walk_air and walk_landed schedule themselves from the live fall's latched polls (UiAction::CaptureAtNextFrame + WindowConfig::end_frame) — the air beat is IN the picture at every pace, requested at frame 104/102 on the day's two runs
last_done="The captures read the latch (loop 456, POORCRAFT 3D): closed STATE's next_task item (1) — the CAPTURE side of the latch cure. (1) THE ACTION (pc3d_render::ui): UiAction::CaptureAtNextFrame { path, ui_dump } joins the proof-hook family (EditSurface/PlayerFace class): a route poll that LATCHED a beat (the landing, the grip, true air) asks for the NEXT presented frame's picture, and the app schedules it into the sorted shot list — the same CaptureOutcome a static Shot produces, at a frame no contended pace can outrun. THE INSERTION LAW (pure, app::next_free_shot_frame + 2 unit tests): a dynamic capture fires requested_at+1, walking forward past any frame a scheduled shot already owns — two shots can never share a frame (the block fires one per frame and frame_no only advances, so the second would silently never fire, and a never-consumed last shot never ends the run). (2) THE HORIZON (WindowConfig::end_frame): a dynamic-capture run's static list may drain long before the last capture fires, so the drain no longer ends such a run — the frame-exact horizon owns the exit (validation: end_frame must exceed every static shot and not precede max_frames, honest errors up front). Runs without end_frame keep the old drain law byte-for-byte. (3) THE WALK-OFF REWIRED: the fixed walk_air@112 and walk_landed@190 shots are gone; the pre-dig rim stays static at 70 (the one deterministic beat) and the two polls now fire the captures — walk_landed AT the landing latch (pose + wounded health + the FELL toast latched, so the toast is fresh in pixels at every pace), walk_air at the FIRST poll genuinely airborne over a meter below the rim (recorded: flag + pose + frame in cells 15..19, latch frame in 20; recordings widened 20 -> 24). The verdict gained the AIR-BEAT assertion: the request pose must hang strictly between the rim's 0.6 m band and floor+0.2, on the spot, BEFORE the latch — a capture that lands on the landed beat now fails even though its pixels still differ from the rim; the air-vs-landed PIXEL difference stays advisory (sway aliasing). (4) PROOF: make p3d-walkoff PASS x2 + comparator at p50 25.5 / 27.4 ms — 'the body fell 56.19 -> 51.19 (deepest air 51.38, air beat latched at frame 104 for frame 105 / 102 for 103) and landed wounded 100% -> 65% (toast true)' — the two runs latched the air beat at DIFFERENT frames (the capture follows the beat, not the calendar) with identical physical claims. Captures INSPECTED x6: walk_air = full health bar + PACK EMPTY + the dig toast (unwounded, pre-landing), walk_landed = the ~64% wounded bar + FELL — HEALTH 64% (em-dash intact) over the pit wall, walk_edge = the rim vista; the layout JSONs carry the same story on disk (landed: both toasts + health 0.647; air: dig toast only + health 1.0). (5) REGRESSION: make p3d-dig PASS x2 + comparator; make p3d-recovery PASS x2 + comparator (p50 26-27 ms); make p3d-people PASS (windowed p50 16.5 ms); p3d-visual-gates ALL 10 PASS; p3d-smoke OK (digest dd019eca900f5a61 UNCHANGED); p3d workspace 686 green / 0 failed (pc3d_render 227 -> 229, the two insertion laws; 14 suites); idle-upgrade-check PASS; root LOREFORGE workspace untouched by this loop (455 verified 480 green at this tree's crates). The six windowed_wild_*.png dirties remain deliberately NOT staged (earlier session's p3d-wilderness run). (6) PERF: no claim, honestly — one Option-compare and a sorted Vec insert per REQUEST (twice per walk-off run), zero cost on any live path; host shared all session (route p50s 16-27 ms), no bench per the 445-455 precedent; the quiet-host re-read stays queued (eleven loops). LORE: canon touched: none — proof-harness capture scheduling over existing routes; no faction, place, event, term, NPC, item, or spell data; no identity assigned. World expression: unchanged — the same fall economy, now framed AT its beats at every host pace. Migration: none (route recordings + proof scheduling; no persisted field, save format, or proof schema change). Deferred honestly: the remaining routes' capture conversions (the playtest's toast-at-capture read, the climb's grip beat, the hang-off's two landings can all adopt CaptureAtNextFrame — their verdicts already read latches, so this is scheduling polish, queued); the quiet-host deck-bench re-read + the bench contention guard; is_water_layer/CTM-strip audit; guardian chronicle re-fire; multiplayer routing of terrain edits; geode pairing; the walk-snap query-bound observation."
next_task: "Carry the list (p3d): (1) LATCH-SCHEDULED CAPTURES, remaining routes — the mechanism shipped in 456 (UiAction::CaptureAtNextFrame + end_frame; walkoff converted and pace-proven). Convert the rest where a beat deserves pixels: the semantic playtest's play_fall_landed (its FELL toast latch exists in a poll window — capture AT it instead of the fixed frame), the climb's grip beat, the hang-off's two landings; each conversion keeps its verdict reading latches and re-runs x2 + comparator. Carried deferrals: the quiet-host deck-bench re-read (queued by 446..456) + the bench contention guard; is_water_layer/CTM-strip audit; guardian chronicle re-fire; multiplayer routing of terrain edits; geode pairing (447's keepers are root-workspace lore); the walk-snap query-bound observation (surface.rs ground_at discards _from_y on the streamed path — the recovery's one-frame lift rides it; if the bound is ever enforced, make the recovery's placement explicit first)."
build: GREEN
tests: "p3d workspace 686 green / 0 failed (14 suites; pc3d_render 227 -> 229: next_free_shot_frame's sorted-insert and never-share-a-frame laws); make p3d-walkoff PASS x2 + comparator (p50 25.5/27.4 ms: fell 56.19 -> 51.19, deepest air 51.38, air beat latched frame 104->105 / 102->103, landed 100% -> 65%, toast true); make p3d-dig PASS x2 + comparator; make p3d-recovery PASS x2 + comparator; make p3d-people PASS; p3d-visual-gates ALL 10 PASS; p3d-smoke OK (digest dd019eca900f5a61 UNCHANGED); idle-upgrade-check PASS"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/walkoff-a/route_walk_off/walk_air.png (full health bar + PACK EMPTY + THE GROUND GIVES WAY — the TRUE mid-air beat, requested by the fall's own poll at frame 104 and captured at 105; its walk_landed twin carries the ~64% wounded bar + FELL — HEALTH 64% captured AT the landing latch)
blockers: "none; proof-harness capture scheduling in app main.rs + pc3d_render ui/app — no lib gameplay, save format, worldgen, or authority change"
