# STATE
loop_count: 350
current_milestone: Complete — first-minute onboarding + pinned starter objective (nightly-beta N01)
last_done: "loop 350 onboarding (N01): a persisted TutorialStep machine (Move→Look→Gather→Craft→Build) in the new lf_client::onboarding module advances only on real gameplay facts — 3 blocks of horizontal displacement, 1.6 rad of camera travel, a natural-material drop (log/dirt/stone/sand) reaching inventory, any hand-craft output, a solid-block placement (torches don't count) — and never skips steps on out-of-order events. The HUD paints a top-center tutorial card (verb, keycap chips from the LIVE keymap via new input::key_glyph, n/5 step chip, click-✕ dismiss) plus a pinned starter-objective line (first incomplete quest + objective progress, own ✕), both drawn by shared painters (ui::paint_onboarding_prompt / paint_pinned_objective / onboarding_*_rect) so the vistest proof pixels ARE the in-game pixels; prompts pause behind modal screens, creative skips them, Gameplay settings gained Show-first-minute-hints + Restart-tutorial. State rides ClientSave (serde default = fresh tutorial; legacy saves migrate to Move), create_world resets it. Also landed the docs/NIGHTLY-BETA goal pack + xtask night-plan-check validator that drives this queue. 422 tests (+12), 96/96 vistest (2 new scenes, both Z.ai-reviewed PASS at 0.97+ confidence), smoke OK, runtimes rebuilt."
next_task: "N02 — transactional workbench and queue correctness (docs/NIGHTLY-BETA/10-OVERNIGHT-JOB-QUEUE.md): make crafting validate/consume/produce atomically across craft-one, craft-all, queue, cancel, full output, rapid input, mod recipe, save/load; property + integration tests BEFORE the N03 presentation pass."
build: GREEN
tests: 422 passed / 0 failed (loop 350)
last_screenshot: shots/vistest_hud_onboarding.png
blockers: "none"
