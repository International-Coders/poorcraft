# STATE
loop_count: 426
current_milestone: WT-007 SLICE 1 LANDED: the GPU marker matrix — the pc3d.* debug-group tree on every frame, live draw-call counting, the ten-field gpu_marker_contract audit (adapter/timestamps/draws), and observatory route_gpu_markers flipped UNAVAILABLE->PASS (5/5 available routes green)
last_done="WT-007 slice 1 (loop 426): the GPU marker matrix. (1) MARKERS: encode_frame pushes the contract tree as real wgpu debug groups — pc3d.frame { pc3d.frame.prepare (the sun-shadow pass, closed before the main pass so passes are siblings) + pc3d.pass.{sky_atmosphere, terrain, npcs, machines (an honest EMPTY slot — sim-side, no GPU pass yet), assets (GLB + flora/crowd/settlement instanced continuation), wireframe_overlay, water, ui (HUD + owner layer) } } and capture_png wraps pc3d.pass.screenshot_readback; marker_log + draw_calls reset per frame and fill live. (2) THE AUDIT: gpu_marker_audit() — build_hash, api_backend + adapter_name (Metal on this host), markers_present vs the 11 required names + the recorded list, timestamp_support with the honest not-enabled/unsupported reason and CPU p50 as the evidence, live draw_calls, asset triangle sum, bucket note. (3) THE ROUTE FLIP: WindowReport carries the final audit; run_observe route_gpu_markers writes gpu_marker_audit.json into the bundle and FAILS if markers are incomplete. EVIDENCE: --observe route_gpu_markers PASS (12 markers, 9 draw calls, Metal, p50 ~36 ms); make p3d-observe 5/5 available routes PASS + 3 honest gameplay-slice UNAVAILABLEs; unit law: marker-tree completeness + all audit fields after one offscreen capture; full suites green. DEFERRED: timestamp queries probed-not-enabled (a future tier), per-module draw counting (noted in the audit), AMD/NVIDIA vendor cookbooks remain docs (the marker tree was their prerequisite). Previous: slice 3 wireframe/overlays (425), WT-003 observatory (424), WT-002 factory (423), WT-001 seed preview (422)."
next_task: "WT-007 SLICE 1 LANDED (loop 426). Next per the WT queue: the gameplay interaction slices that flip the three remaining observatory routes — NPC talk (route_npc_talk), forge use (route_forge_use), house entry (route_house_entry, WT-002 slice 5) — each with real interaction mechanics + UI + proofs; then WT-008 data extraction in code. Keep the owner play-pass gate for the stamped DMG as the separate manual gate."
build: GREEN
tests: "marker-tree law 3/3 (completeness + audit fields), --observe all: 5/5 available PASS (gpu_markers: 12 markers, 9 draws, Metal) + 3 honest UNAVAILABLE, full p3d + root suites green"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/observatory/route_gpu_markers/beauty_markers.png (the gpu-markers route frame; the bundle carries gpu_marker_audit.json with the 12-marker tree + 9 draw calls)
blockers: "none; unrelated dirty render/app/asset-capture files were left untouched for their owning work"
