# STATE
loop_count: 452
current_milestone: THE PACK IS LEGIBLE: the inventory's echo — after one real G press through the live dig verb, the HUD's carried-stock line under the status bars reads PACK WOOD 1 · SOIL 1 (the middle dot now has a glyph), the number rides every gameplay frame and follows the whole economy (harvest → forge → pack), and the route law pins the exact before/after strings
last_done="The pack is legible (loop 452, POORCRAFT 3D): closed STATE's next_task item (1) — the INVENTORY ECHO of the dig verb, finished from an interrupted session's orphaned start (items.rs held an unwired, untested stock_line; the worktree dirty was reconciled, completed, and proven). (1) THE LINE (pc3d_world::items::stock_line, kept from the interrupted start): the inventory's echo as ONE stable string — nonzero kinds in CATALOG order (not insertion order — the unit law caught my own test assuming insertion order), counts summed across stacks, 'PACK WOOD 5 · STONE 12 · SOIL 1' shaped, an empty pack reading 'PACK EMPTY'; two new unit laws (p3d501_stock_line_names_nonzero_kinds_in_catalog_order, p3d501_stock_line_carries_the_dig_take) — pc3d_world 265 -> 267. (2) THE HUD (pc3d_render::ui): HudValues.stock synced by the app FROM THE REAL slice inventory on every dirty frame (all inventory mutators mark the UI dirty; an idle HUD pays nothing — no per-idle-frame string), painted verbatim under the status bars as hud_stock, serialized to the inspector JSON ('hud'.'stock'); a layout law pins it under the bars, inside the safe margins, clear of the hotbar, and ABSENT until synced — pc3d_render 222 after this law. (3) THE GLYPH, a proof-found fix: the captures INSPECTION showed the ' · ' separator rendering as a BLANK GAP — '·' was not in the pixel font and unknown chars fall back to space silently (the pre-existing prompt 'F BUILD SAND · R REMOVE' had the same hole). Added the middle-dot glyph (a 2x2 dot, original) + an ink law (the_middle_dot_rasterizes_with_ink) — the pack line AND the prompt row now read with their separators. (4) THE EVIDENCE PATH fix: CaptureOutcome.ui_layout carried the PRE-merge layout (ui_state existed only in the on-disk dump), so verdicts and dumps read different structures — the capture now merges the LIVE ui_state into the in-memory layout too, one structure for both. (5) THE ROUTE (make p3d-dig extended): the arm computes the EXPECTED pack line from the SAME determinants the verb uses (the spot cell's material through harvest_yields over an empty inventory — the steep aim takes the body's own column) and the verdict asserts the HUD strings from the captures' layouts: dig_before 'PACK EMPTY' -> dig_after the yield's own line; the verdict line prints the echo ('pack PACK EMPTY -> PACK WOOD 1 · SOIL 1'). PASS x2 identical + comparator PASS; all four captures INSPECTED (PACK EMPTY; the take + DUG SOIL+WOOD toast over the dug seam; on the terrace; back on the rim, pack persisting). (6) THE WIDER ECONOMY proven by the playtest's own journey: the pack line reads IRON_ORE 4 + WOOD_PICK + BREAD 2 after the harvest, IRON_ORE 2 + IRON_BAR 1 after the forge take, IRON_ORE 4 + IRON_BAR 1 after the re-harvest — play_taken.png INSPECTED (the line legible over the open forge/journal panels). (7) EVIDENCE: p3d workspace 680 green / 0 failed (pc3d_render 223 = 221 + pack-line layout law + middle-dot ink law; pc3d_world 267); make p3d-dig PASS x2 + comparator PASS; make p3d-playtest PASS x2 + comparator PASS, bundle digest CHANGED 05c46411869a857c -> 1d37f62c3801ae20 (HONEST change: the pack line rides every gameplay capture — re-baselined this loop); make p3d-people PASS (12 NPCs, 7 draws, 92 instances, frozen-stride motion 0.64%); p3d-visual-gates ALL 10 PASS (windowed-3d-axes/terrain/stream/water/city/npcs/quality/slice/ui-states/assets — the ui-states gate carries the new hud_stock element inside its safe-margin law); p3d-smoke OK (digest dd019eca900f5a61 UNCHANGED — headless, no HUD); idle-upgrade-check PASS. (8) PERF: no claim, honestly — the sync is one small string on UI-DIRTY frames only (every mutator marks dirty anyway; an idle HUD pays nothing), the paint is one text row, the glyph is a 12-pixel dot; the per-frame walk/stream/crowd paths are untouched; host shared (windowed p50 19.5-23.0 ms across this loop's routes), no bench per the 445-451 precedent; the quiet-host re-read stays queued (seven loops). LORE: canon touched: none — a read-only echo over the existing generic material catalog; no faction, place, event, term, NPC, item, or spell data; no identity assigned. World expression: labor in Valdenmoor leaves a legible trace — what you dig is what you carry, as a number the item authority owns; the dig verb's loop is closed with the forge/deliver economy the same line serves. Migration: none (read-only echo; no persisted field, save format, or proof schema change; the captured layouts carry one more hud key). Deferred honestly: the lethal plaza-recovery branch route (next_task item 1); Space jump-off from a hang; the quiet-host deck-bench re-read + the bench contention guard; is_water_layer/CTM-strip audit; guardian chronicle re-fire; multiplayer routing of terrain edits; geode pairing (447's keepers are root-workspace lore)."
next_task: "Carry the list (p3d): (1) The lethal plaza-recovery branch route (the fall law's 0.5-health plaza recovery has no windowed route). Carried deferrals: Space jump-off from a hang (unit-lawed only); the quiet-host deck-bench re-read (queued by 446..452) + the bench contention guard; is_water_layer/CTM-strip audit; guardian chronicle re-fire; multiplayer routing of terrain edits; geode pairing (447's keepers are root-workspace lore)."
build: GREEN
tests: "p3d workspace 680 green / 0 failed (pc3d_render 223 = 221 + pack-line layout law + middle-dot ink law; pc3d_world 267 = 265 + 2 stock_line laws); make p3d-dig PASS x2 + comparator PASS (pack PACK EMPTY -> PACK WOOD 1 · SOIL 1; captures inspected); make p3d-playtest PASS x2 + comparator PASS, bundle digest RE-BASELINED 1d37f62c3801ae20 (the pack line rides every gameplay capture — honest change); make p3d-people PASS (12 NPCs, 7 draws, 92 instances, motion 0.64%); p3d-visual-gates ALL 10 PASS; p3d-smoke OK (digest dd019eca900f5a61 unchanged); idle-upgrade-check PASS"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/dig-a/route_dig/dig_after.png (the HUD pack line reading PACK WOOD 1 · SOIL 1 under the status bars, over the dug seam and the DUG SOIL+WOOD toast — from this loop's green make p3d-dig run)
blockers: "none; a read-only UI echo over the existing item authority — no save format, worldgen, or asset change"
