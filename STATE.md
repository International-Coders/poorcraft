# STATE
loop_count: 471
current_milestone: THE PEER SEES THE DIG LAND: the two-client route over real UDP (loop 471) — the GPU-side end of the wire laws, carried since loop 450 and TOP since 470 closed the consumption tiers. Until now every multiplayer law was proven message-deep; no proof ever showed a second client's RENDERED PIXELS carrying another player's edit (loop 462's honest limit: "the multiplayer GPU client has no visual harness"). THE DRIVER: GameState::new_headless — the surfaceless render path STATE scoped for: the real client constructed with no window/surface/egui/audio (everything else byte-identical to the windowed boot), driven through the same tick the windowed loop runs, read through the same headless render take_screenshot uses — NO client logic is duplicated anywhere (the join is the UI's connect, the dig is the mining input's host_set_block call, the witness's sight is the factored snapshot_image). THE ROUTE (one law in the standard suite): real dedicated Server::start on an ephemeral port → the WITNESS joins (Welcome seed adoption, the join-identity law) → sights a bare-hand-diggable cube (the SAME gate the server applies) and aims its player camera → the BEFORE picture → the DIGGER joins and digs through the host funnel (MineClaim on the wire) → no-self-echo proven from the editor's own socket (no BlockUpdate, no Reject, the ItemGrant arrives) → the witness's world takes it, its chunk remeshes, and the PIXEL GATE passes: the ROI where the column was aimed moves strongly (mean |d| > 8, >1500 pixels over 40) — the hole is IN the picture, INSPECTED (intact grass cube before, a clean 1×1 notch with shadowed interior after). THREE PROOF-TRACED NONDETERMINISM CURES, each found by re-running the route and pinned where it bites: the view distance pinned to the boot ring BEFORE the join (the adoption's streamer never requests beyond it — wall-clock chunk arrival can never leak into the pixels); the pose pinned immediately before each snapshot (physics settle leaves sub-ulp eye drift that flips scattered rasterization pixels); the wind frozen by the low tier's own law (particles off → env.time 0.0 — foliage swayed with the wall clock) + weather pinned. The committed PNGs rewrite byte-identical across runs AND across debug/release profiles.
last_done="The peer sees the dig land — the two-client route over real UDP (loop 471, root LOREFORGE): closed STATE's next_task item (1) — the windowed two-client route, the multiplayer authority program's remaining proof. THE DRIVER (lf_client): GameState::new_headless (new_inner shared with the windowed constructor — one boot, parameterized surface): window/surface/egui Option-ified with guards at every consumer (cursor lock/unlock, update_title, resize, the tick UI pass, the render pass, the App event path); the surfaceless driver arms no audio output; icons get a standalone egui context; take_screenshot's body factored into snapshot_image (sorted chunk order — a HashMap's iteration order shuffled the submission order run-to-run and flipped scattered edge pixels in the readback). THE ROUTE (lf_client tests, dev-dep lf_server — no cycle): the_server::start(127.0.0.1:0, 4242) → witness new_headless + NetClient::connect + tick to Welcome adoption → plant on the adopted surface, ui_open None (the player camera, not the title orbit) → sight scan (solid+opaque+air-above+required_tool None — the server's own gate) → BEFORE png → digger joins → host_set_block(AIR, Mine) → no-self-echo + ItemGrant on the digger's socket → witness world AIR → mesh hash differs → AFTER png → ROI pixel gate (mean |d| > 8.0 AND >1500 strong pixels; a refused or lost edit leaves the ROI identical). PROOF-TRACED CURES: deterministic sight (view_distance 1 before the join; route_plant_sight pose pin before each snapshot; particles off + weather false — env.time was self.elapsed, wall-clock foliage sway). REGRESSION: cargo test --workspace 552 green / 0 failed, exit 0 (xtask's 12 included = loop 470's 551 + 1); make smoke OK; make twoclient (release) PASS in 2.06 s with PNGs byte-identical to the debug runs; FULL vistest battery 110 scenes [ok] / 0 FAIL exit-0 across TWO runs with every committed PNG byte-identical (md5 462b2318ed4c98d9a882266cd81fbd7d pre == mid == post) — singleplayer render paths pixel-proven unchanged; --features steam compiles clean; POORCRAFT 3D builds untouched; runtimes refreshed (dist/ dmg 8.9 MB hdiutil VALID + linux tarball 8.5 MB + .app binary 20 MB + server); Windows exe honestly skipped (mingw absent); the six windowed_wild_*.png dirties remain deliberately NOT staged. PERF: not applicable — proof-only work (one test, run on demand); the windowed client's paths are unchanged (branch-on-Option guards), pixel-proven by the battery digest. LORE: canon touched: none — multiplayer proof plumbing (the 462/465-470 precedent); the route's ground is the realm's own shared terrain; no faction, place, event, term, NPC, item, or spell data changed; no new canon text. Canon preserved: the chronicle as the player-authored history; Anima as a material energetic property; no identity assigned. World expression: in shared Valdenmoor what one adventurer breaks is seen gone by another — the route proves the realm's shared ground is real in the picture a peer actually renders. Migration: none — no wire change (v10 stands), no save format, no block/item change, GENERATOR_VERSION unchanged, ClientSave untouched."
next_task: "Carry the list (root): (1) The lying-PackSync bootstrap tier — a modified client's claim can re-seed the ledger (named at 466, unchanged since); full closure = server-side mode authority + a spawn inventory the server seeds (protocol v11: the server grants the joiner's mode and starting kit instead of trusting the Hello claim). Now the TOP carried item with the route shipped. (2) Route extensions now that the two-client route exists: the peer SEES a placement land (the place-payment law's visual end), and/or two real clients trading through the escrow (the trade laws' visual end). Check STATE/build first: prefer the higher-severity item per the priority ladder."
build: GREEN
tests: "cargo test --workspace 552 green / 0 failed, exit 0 (= loop 470's 551 + 1: the two-client route law); make smoke OK; make twoclient release PASS (2.06 s, PNGs byte-identical to debug); vistest battery 110 scenes exit-0 twice, all committed scene PNGs byte-identical (md5 462b2318ed4c98d9a882266cd81fbd7d)"
last_screenshot: "shots/twoclient_peer_before.png + shots/twoclient_peer_after.png (loop 471, the route's own evidence — INSPECTED: intact grass cube before, a clean 1x1 notch with shadowed interior after; byte-identical across two debug runs and the release run). The newest visual scene remains shots/vistest_resonance_compass_hud.png (loop 464)"
blockers: "none; the six windowed_wild_*.png dirties are NOT this loop's and remain unstaged"
