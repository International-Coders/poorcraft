# STATE
loop_count: 454
current_milestone: SPACE LETS GO: the strand release is real — the Space hop-off rises off the strand, falls PAST it, and lands the fall law's exact wound; the S tip release falls free and lands safe (both silently re-gripped before: the windowed probe caught the strand acting as a one-way trap under its own toast)
last_done="Space lets go, live (loop 454, POORCRAFT 3D): closed STATE's next_task item (1) — and the route the loop wrote found a REAL BUG before shipping anything. (1) THE BUG, pinned by the pre-law windowed probe on UNCHANGED code: both strand release paths silently re-gripped. The Space hop-off rose 1.04 m (peak 56.95 from hang 55.91), fell straight back into the strand's crossing catch (window-min 55.86 vs tip 54.84 and ground 53.18 — never past the strand; the landing latch never fired in 245 polls), and the S tip release was re-caught INSIDE the grab band below the tip (the body pinned AT the tip 54.84, health 1.00, never grounding). The strand was a one-way trap: the 'SPACE LETS GO' toast fired and the strand caught the body anyway. The old unit suite could not see it — the_catch_saves_the_body modeled no post-release grab check. (2) THE LAW (pc3d_render::app): SliceHost.released_from records the strand a body let go of (Space hop-off or tip release); the arc's grab check refuses THAT strand through the pure gate strand_can_catch — strand-scoped (a DIFFERENT strand in hand reach still catches), spent on landing, spent by a new grab; the grab law's own tunnel/crossing tests untouched (a strand a body never held still catches any fast drop). 3 new unit laws — the Space hop-off composition (rise ~1 m, falls past its own strand, pays the height law's exact wound), the tip release (falls free of the band that used to re-catch it; the old re-catch pinned as the forbidden behavior), strand-scoping + spent-on-landing. pc3d_render 224 -> 227; p3d workspace 681 -> 684 green / 0 failed; root workspace 480 green. (3) THE ROUTE (make p3d-hangoff, route_vine_hangoff): two legs on the climb route's own searched strand. LEG 1: drop 12 m onto the line (caught), Space pressed ONCE through the real key path — the hop RISES (peak = hang + 1.04), the body falls PAST its own strand (window-min = the ground below the tip), lands GROUNDED at the strand's line, and pays EXACTLY the wound the recorded heights name through the impact law (health 100% -> 81-91% == the law's number at every pace tested). LEG 2: drop again, descend PAST THE TIP with S — the release falls free and lands SAFE (health never drops; regen may only heal). ALL landings latch on two consecutive on-ground polls against the LIVE ground answer and the FELL toast is latched AT the landing — pace-proof by design: the route PASSED at 20, 30, 48, and 80-108 ms p50 across the day's host states. (4) THE CLIMB ROUTE TELLS THE TRUTH NOW: its verdict asserts GROUNDEDNESS (the pre-law run ended with the body pinned at the tip — the old 'landed unharmed' claim passed without checking) and proves the fall/catch by latched records (the drop's lowest height, the hang inside the strand's own span, the GRIPPED toast latched during the poll) instead of the frame-vs-frame pixel bar that measured sway aliasing, not the fall, at contended paces (evidence: the same frame pair differed >0.02 at 22 ms and ~0 at 80-100 ms). Climb PASS x2 + comparator at 79-88 ms. (5) THE FOREIGN-LOAD SPIKE, honestly: mid-session the host loaded to a 94 15-minute average (outside processes; windowed p50 54-142 ms) — the frame-CALIBRATED walkoff and playtest chains broke on re-run (walkoff at 54 ms: the fall completed before its frame-112 window, the FELL toast expired, fed-body regen healed the wound past the band — the documented 447/449 capture-window sensitivity, THIRD occurrence). Those two routes' code is untouched by this job; their bundles ride from loop 453 (git HEAD, same route code) and the playtest bundle digest reproduced UNCHANGED 05c46411869a857c on this job's dev-stamped builds pre-spike, pinning non-perturbation. dig re-ran green x2 + comparator at 20.0 ms pre-spike; people PASS (motion 0.64%); gates ALL 10 PASS; smoke OK (dd019eca900f5a61 UNCHANGED); idle-upgrade-check PASS; the six windowed_wild_*.png dirties belong to an earlier session's p3d-wilderness run and are deliberately NOT staged. (6) PERF: no claim, honestly — the gate is one Option<[f32;2]> compare per falling frame; the routes add work only while they run; the live walk/stream/crowd paths are untouched; the host was contended all session (no bench per the 445-453 precedent); the quiet-host re-read stays queued (nine loops). LORE: canon touched: none — a traversal law over the existing vine strand; no faction, place, event, term, NPC, item, or spell data; no identity assigned. World expression: in Valdenmoor a hand that lets go is free — the strand catches a FALL, never holds a body that released itself; the fall economy of 441/444/446 remains the only judge of every drop. Migration: none (in-memory slice state + route records; no persisted field, save format, or proof schema change). Deferred honestly: the windowed harness's frame-scheduled captures stay pace-sensitive — latch-scheduled captures are the systemic cure (next_task item 1); the quiet-host deck-bench re-read + the bench contention guard; is_water_layer/CTM-strip audit; guardian chronicle re-fire; multiplayer routing of terrain edits; geode pairing; the walk-snap query-bound observation (surface.rs discards _from_y)."
next_task: "Carry the list (p3d): (1) THE WINDOWED HARNESS'S FRAME-SCHEDULED CAPTURES — third occurrence today: the walkoff/playtest chains broke again under a foreign load (p50 54-115 ms; walkoff's frame-112 mid-air window, the 3 s toast reads, and the regen bands are all calibrated to the 20-34 ms band). The systemic cure is LATCH-SCHEDULED CAPTURES: a UiAction (e.g. Capture::at_next_frame(path)) the routes fire from the same latched records the verdicts already trust, instead of pre-fixed frame indices — then re-time walkoff/playtest onto it and re-run them green at any pace. Carried deferrals: the quiet-host deck-bench re-read (queued by 446..454) + the bench contention guard; is_water_layer/CTM-strip audit; guardian chronicle re-fire; multiplayer routing of terrain edits; geode pairing (447's keepers are root-workspace lore); the walk-snap query-bound observation (surface.rs ground_at discards _from_y on the streamed path — the recovery's one-frame lift rides it; if the bound is ever enforced, make the recovery's placement explicit first)."
build: GREEN
tests: "p3d workspace 684 green / 0 failed (pc3d_render 227 = 224 + 3 release laws; pc3d_world 267 unchanged); root workspace 480 green; make p3d-hangoff PASS x2 + comparator (PASS at 20/30/48/80-108 ms: hop rose 1.03-1.04 m, fell past the strand, grounded at the strand's line, wound == the law's number; tip release fell free, landed safe); make p3d-climb PASS x2 + comparator (79-88 ms, grounded, span records); make p3d-dig PASS x2 + comparator (20.0 ms); make p3d-people PASS (12 NPCs, 7 draws, 92 instances, motion 0.64%); p3d-visual-gates ALL 10 PASS; p3d-smoke OK (digest dd019eca900f5a61 UNCHANGED); idle-upgrade-check PASS; walkoff/playtest bundles ride from loop 453 (untouched route code; playtest digest reproduced UNCHANGED 05c46411869a857c pre-spike)"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/hangoff-a/route_vine_hangoff/hop_landed.png (grounded at the strand's line with the FELL — HEALTH 80% toast and the wounded bar over the strand's clearing — from this loop's green make p3d-hangoff run)
blockers: "none; a slice-local release guard + one pure gate + the hang-off route — no save format, worldgen, or authority change"
