# STATE
loop_count: 413
current_milestone: GLM UI REWORK EXECUTED (UI-001..UI-008): the owner-facing game now has a REAL UI — title/new world/load world/settings screens with mouse+keyboard navigation, a pause menu with explicit quit choices, confirmation modals that own the frame, a live gameplay HUD (health/stamina/food bars, XP strip, 9-slot build-palette hotbar where selection changes what F places, crosshair, prompt, fading toasts), an F3 debug strip hidden by default, settings that drive the runtime (sensitivity, invert Y, FOV, UI scale, quality) and persist, save slots with seed+date and load/delete confirmations, an 11-scene screenshot harness with pixel checks + layout dumps (--ui-shots), and a local JSON inspector (--ui-inspect) whose replay proves Escape pauses and never exits
last_done="GLM UI REWORK EXECUTED (UI-001..UI-008, the full pack queue): pc3d_render::ui (new, 26 tests) is the owner UI layer — UiState (explicit serializable screen/focus/hover/modal/settings/HUD state), a pure layout builder with the SAFE-MARGIN + fit-scale + no-interactive-overlap laws at 1280x720/800, 1920x1080, 2560x1440, a pure CPU painter (forged panels with bevels+studs, ember focus/hover rings, chunky gradient logo on a backing band, live bars with sheen, 9-slot hotbar with material swatches, crosshair, toasts, F3 debug strip), and a pure input reducer whose UiActions the app executes (save/load/delete/create/settings/quit all behind confirmations). The renderer draws the canvas as one fullscreen alpha-blended quad AFTER the world (set_ui_layer; uploads only on change; blank hud line = no debug draw at all). The bitmap-text owner overlay is REPLACED: real Title/NewWorld/LoadWorld/Settings/Pause screens with mouse + arrow-key/Enter navigation, modals that own the frame (dim + modal only — the overlap law forced it), save slots listing saves3d with seed+date (framed-meta parse at offset 24, regression-tested), settings that drive runtime LIVE (sensitivity/invert-Y in the look handler, FOV->camera, UI scale->layout, quality->deck::apply) and persist to saves3d/settings.json. The HUD is live state: stamina drains while walking/recovers at rest, food drifts with travel, health honest-full until damage exists, XP = first-build marker, hotbar slots 1-5 are the REAL build palette (selection changes what F places), 1-9/wheel select, prompt + fading toasts. PROOFS: --ui-shots / make p3d-ui-shots captures 11 scenes (title 720p + focus variant, new world, load world, settings, gameplay default + debug values, pause over bright sky, modal over dark terrain, debug inspector, deck 1280x800) each with a layout dump and in-process pixel checks (nonblank, safe margins, no overlap, alpha presence, required/forbidden elements, focus distinctness 2092 px, health fill 0.38~0.45) — ALL PASS; --ui-inspect is the local JSON inspector (ui_state/input_state/set_screen/set_debug_hud_values/capture_screenshot/dump_ui_layout/dump_frame_stats/dump_visible_world/dump_mesh_wireframe/replay_input/wait) whose 12-command proof replays Escape TWICE: pause+blocked+alive, then resume+regrabbed — ok:true; the visual battery gained the ui-states gate (capability inventory + guardrail updated together: 10 gates, owner_ui renderer path declared); p3d 457/457 (+30), root 474/474; no-args alive 12 s on the title screen; DMG rebuilt and its route proof PASSED from the mounted read-only volume. BUGS the proofs caught: script actions did not set the screen (executor now idempotent), multiple ui_script steps at one frame never fired (drain loop), settings +/- minis overlapped at scale 1.5, the vision pass caught the legacy debug line behind the title (blanked), save-slot seeds parsed the frame length not the payload (offset fix + test). Human inspection: all 11 scenes read at native resolution (notes in docs/POORCRAFT-3D/GLM-UI-REWORK-PROOF.md). Honest deferrals: hotbar 6-9 reserved (no tools yet), health has no damage source, dump_mesh_wireframe is summary-level v1, no audio/rebinding/thumbnails, Deck hardware pass pending. THE OWNER'S MANUAL PLAY PASS REMAINS THE GATE. Previous: NWR-011 — the rebuild vertical slice: assemble_rebuild mounts the FULL stack on the deterministic showcase route (seed 22, 1.9 s: capital + town + river + corridor cave) — streamed surface terrain, Mid atmosphere, instanced wilderness, the settlement kit with river dock and water wheel, the rigged crowd on the real schedule, conforming water, the cave mesh — with the player WALKING THE SURFACE (SurfaceStreamer: CollisionSurface; the NWR-005 deferral lands) and F building only through INSPECTED foundations (rejections name the reason). Proofs: five route captures (vantage/street/river/cave/gate) with probes at p50 9.86 ms / 104 fps, the construction save/reload round trip proven in-arm, the live smoke alive 12 s, human inspection PASS on the vantage (banner-topped keep + town in haze) and gate (evergreens + standing stones at player height), REVIEW-CHECKLIST.md with the explicit OWNER GATE (not a commercial beta until the human owner passes it in person). 9/9 gates; p3d 421/421, root 474/474. Honest deferrals: live terrain-edit persistence on B, cave live collision, rotating water wheel — and THE OWNER'S MANUAL PLAY PASS remains the gate before 'playable'. Previous: NWR-010 — the quality experience became a single TRANSPARENT contract. pc3d_render::deck (new) composes every tier lever into named Low/Mid/High presets — streaming rings, mesh work/frame, GPU byte budget, shadow map, fog, material detail, water glint, flora + grass radii, and a NEW crowd pose-rate lever (Low 15 Hz, quantized so frozen clocks stay deterministic) — plus honesty rows: render scale declared 1.0 (no scale-swap path exists) and the same-world/interactions law (anchors, characters, water, edits visible at every tier; no lever swaps the world). The BENCHMARK: --deck-bench / make p3d-deck-bench walks four waypoints over the FULL stack (streamed terrain + flora + settlement kit + 12-NPC crowd + atmosphere) per tier — one process per tier (winit allows one event loop; the three-in-one attempt failed loudly), CSV sidecars, and a report invocation assembling docs/.../DECK-BENCH-REPORT.md with the contract table, measured rows, and bottleneck notes, enforcing the LOW-NOT-SLOWER law. MEASURED (800x500, documented Apple host iGPU evidence machine): low p50 4.88 ms / 167 fps / 5.1 MB GPU / 176 patches; mid 6.50 / 149 / 18.9 MB / 402; high 6.66 / 146 / 27.6 MB / 603 — the levers measurably behave and the per-frame caps HELD every run; the low capture human-inspected PASS (characters, crofts, treeline readable). 3 deck tests (completeness + monotonicity + readability laws); 9/9 gates; suites p3d 421/421, root 474/474. Honest deferrals: no internal render-scale path (declared 1.0), CPU/GPU-time split unobtainable without a profiler (percentiles + work counters are the evidence), mid-ring mesh sampler optimization remains future work with its capped behavior documented. Previous: NWR-009 — the NPCs graduated from static posed boxes to an original low-poly RIG with a deterministic animation state machine driven by the AUTHORITATIVE intent. rig_pose(brain, t) is pure f(intent, time): Walking swings counterphase legs/arms with the body yaw FACING THE PATH LEG read from the sim's own path; work activities stroke the right arm; Sleeping lies low; Idle breathes. Role variants read at a glance — GUARD steel helm + spear at every activity (the role's identity), WORKER tool ONLY while Working (an idle worker never looks busy), RESIDENT satchel; beyond 64 m single-box impostors. The crowd draws through a new inst_box pipeline (unit cube + PER-AXIS instance scale), one bucket per part color — a crowd of ANY size is <= ~8 draw calls (12-NPC proof: 7 draws / 92 part instances); poses rebuild per frame from the shared frozen-able clock; positions ALWAYS npc_world_pos(brain) — no visual NPC simulation; crowd_tick advances the real brains; CrowdGround gives chest-high capsule collision. Proofs: 4 rig tests (determinism + intent laws, sim-following instances, the capsule stop, GPU control-diff 0.0725 + animation 0.0112 between frozen times) + WINDOWED --play-people / make p3d-people (151 frames p50 ~6 ms with atmosphere + flora + settlement kit + crowd): plaza/stride/guard/anchors captures human-inspected PASS (limbed people in the lane, the full guard rig close-up, anchor frames inspectable; stride frames differ 0.35%); the existing npc-cast gate stayed green untouched. BUGS the proofs caught: the per-frame pose clock clobbered directly-poked poses (set the frozen time, don't poke); a 0.16 m stride is sub-pixel at 13 m (camera 3 m from the walker); the sleeping-height law measured role GEAR not the body. 9/9 gates; suites p3d 418/418, root 474/474. Honest deferrals: no skinned meshes, no NPC shadows in the sun pass, chunked updates only for very large crowds, NPC avoidance is the sim's business, live-shell rig attach at NWR-011. Previous: NWR-008 — the visible city graduated from procedural prisms to the FIRST original modular settlement kit. tools/assetgen ships ten GLBs with DECLARED SOCKETS in the GLB nodes (house, workshop, market stall, wall segment chaining wall_a/wall_b, the gate arch with passage + wall flanks, watchtower, keep with banner_top, bridge/dock, banner sign, water wheel — byte-identical regen within budgets; the wheel's doubled spoke layer was caught by the 500-tri budget and trimmed). pc3d_render::settlement assembles them from the AUTHORITATIVE castle/settlement plans: an EXHAUSTIVE kind->module mapping (compiler-enforced completeness), one placement per plan element at its footprint center, walls chained socket-to-socket along the 3 m circuit, collision/nav/anchors from mesh_city PARITY with the gate REFINED so its passage column opens (the primitive path blocked the whole footprint), Bed/Work/Idle zones as colored markers, and a river dock + water wheel when one is in range — drawn INSTANCED through the flora pipelines (8 bucket draws / 620 tris at street vantage) with sun shadows. Proofs: plan-to-render consistency + assembly determinism (the save/load stability law), wall socket chains <0.35 m, collision/nav parity with exactly the gate passages opened, the player WALKS THROUGH the gate and is stopped by walls (purpose-built flat stage; the proof capital's own gate approach carries a real terrain step — honestly noted as plan/foundations work), GPU control-diff 0.019 with 12 LOD buckets. WINDOWED --play-settlement / make p3d-settlement (141 frames p50 4.81 ms with Mid atmosphere + flora + kit): overview + street captures human-inspected PASS — crenellated wall circuit with towers, banner-topped keep, thatched crofts along a lane, market stalls, cast shadows in the haze. BUG the proofs caught: the collision adapter's height window rode GENERATOR terrain — on flat stages walls turned ghost and the walk sailed through one; the window now rides the inner surface's own ground answer. The inventory guardrail recorded the ten new GLBs and unions all three packs. 9/9 gates; suites green. Honest deferrals: gate-approach terrain leveling (foundations), a real well piece (banner stand-in), road rendering, module texture variants, live-shell kit attach (NWR-011). Previous: NWR-007 — the world grew its wilderness. pc3d_world::flora (new, PURE) is the authoritative WHAT-GROWS-WHERE: nine plant kinds on a 4 m slot grid, each slot an FNV hash of (seed, slot) gated by a per-biome density table (Forest treed, Plains grassland, Mountains rocky, Ocean nothing); trees keep 8 m spacing on the even-diagonal subgrid and refuse steep slopes; one standing-stone LANDMARK per region where a ~18% hash gate and gentle ground agree (44/256 regions on the proof seed); jitter() feeds the renderer variation from the same authority. pc3d_render::flora draws it INSTANCED — ONE draw per (kind, LOD) bucket (17 buckets, ~830 instances in the proof) — with wind-animated grass cutout cards, LOD by the glb thresholds, a BOUNDED rotating slot-scan budget with cached negatives, eviction beyond ring+16 (teleport/reload proven: the SAME ring returns), instance sun shadows, and a FloraGround collision adapter derived from the authority (the walk test stops at a trunk and passes clear ground — no ghosting by construction). assetgen shipped nine original GLBs (pine/broadleaf/birch trees, boulder/spire/slab rocks, shrub, fallen log, standing-stone landmark, all byte-identical regen within budgets) and wilderness_batch.json validates against files on disk (schema v2 gained flora/landmark categories rather than weakening the id law). Proofs: world placement laws (determinism, spacing, searched-region biome character: Forest 446 trees vs Plains 43), GPU control-diff 0.25 under atmosphere Mid + wind 0.0077 + LOD buckets, WINDOWED --play-wilderness / make p3d-wilderness (191 frames p50 3.88 ms / avg 333 fps WITH atmosphere + ~830 instances; Deck-low tier = 84 m ring / 22 m grass): vista + landmark captures human-inspected PASS (evergreens and a hazy treeline; the monolith with plinth and flank stones amid swaying grass), 3% diff vs the no-flora control. 9/9 gates; suites p3d 405/405, root 474/474. TWO streaming bugs the settle test caught and fixed: the scan restarted at ring 0 every call (outer rings NEVER reached under the budget — rings now rotate by phase) and empty slots were re-queried every call until negatives were cached. Honest deferrals: per-plant frustum culling, grass collision (never blocks by contract), landmark on the single-asset slot, GLB cutout leaves, live-shell flora attach (NWR-011), grass far-LOD. Previous: NWR-006 — the renderer gained its stylized atmosphere layer, all tier-budgeted and all OFF by default: the renderer starts at LEGACY (every term an identity) so every pre-NWR-006 proof stays bit-identical and all 9 visual gates re-ran green untouched. pc3d_render::atmosphere (new) ships AtmosphereTier Low/Mid/High with documented budgets (Low: no shadow pass + thin fog; Mid 1024^2 depth +~4 MB, fog 0.0055, material detail 0.55, glint; High 2048^2 +~16 MB, fog 0.0070, detail 0.85), the TEXEL-SNAPPED sun shadow map (depth-only pass + cutout variant, 3x3 PCF with normal offset and constant+slope shader bias, 0.35 soft floor; a still camera yields a bit-identical map frame — unit-asserted), exp2 distance fog mixed in linear space (CPU mirrors are the probe truth), the 4-tile procedural material atlas (grass/ridged rock/sand/snow sparkle) generated from pc3d_assets::material_detail metadata and blended by albedo-derived weights (no vertex format change, no image files), a capped water sun glint, and the alpha-cutout foliage pipeline with deterministic nibbled leaf masks + crossed cards (Solid control mask). Proofs: 13 atmosphere tests — shadows darken 0.35 exactly at the computed cast point (mean frame delta 0.004, local; sun-side unchanged; bit-stable), fog pulls the far field to haze (0.70 -> 0.07) keeping the near ground, grain 2 -> 81 (grass) / 108 (rock) distinct colors with separation + hues preserved, glint 2.36 -> 2.84, cutout shows the wall through the holes (3485 vs 3361 control px) — plus the WINDOWED --play-materials / make p3d-materials (171 frames p50 0.70-0.84 ms): mid/legacy/high/cutout captures human-inspected PASS (cast shadows behind foliage, haze swallowing the far hills, sun glint streak on the water, mottled grass vs gray rock, see-through leaf cards), atmosphere diffs 34%/39% vs legacy. FIVE real bugs the proofs caught and fixed: RH ortho depth sign (view looks down -z), SUN_DIR points TOWARD the sun (light camera looks along -SUN_DIR, eye up-sun), the shadow-lookup v-flip vs NDC y (everything-shadowed run), float-depth pipeline bias units are whole-range (constant 2 = always lit; bias now shader-side constant+slope), and the wgpu usage-scope law (the shadow pass must not sample the texture it writes — bg_light). Honest deferrals: one cascade (near field), sky-band fog, solid foliage shadows, GLB textured leaves (NWR-007), live-shell tier binding (NWR-011). Previous: NWR-005 — the surface path gained sparse caves, terrain-conforming water, and foundations. pc3d_render::world_features (new): CaveRegion grows a carved connected component from final_solid (the ONE authority) and meshes it by face-net extraction — quads exactly where solid faces carved, boundary faces from the same authority, so the cave welds to the surface with no duplicate faces or daylight leaks; ConformingWater draws one strip per river edge from the authoritative RiverGraph/FlowTable (width/speed from real discharge) whose 17 heights follow the surface patches + edit deltas, water_line = strip min - 0.45, banks where terrain rises, refresh_after_edit touching ONLY sections near the edited patches (the P3D-303 dirty law); check_foundation returns Valid{leveled_by}/Rejected{named reason} from walkable+slope+corner-level checks on live ground. PlayerBody now walks any CollisionSurface (new trait; SurfaceRegion implements it — walking the surface and feeling a raise underfoot is test-proven; the legacy authority path unchanged). Proofs: 5 unit laws + GPU cave_water_and_foundation_render_and_edit_locally (cave interior control-diff 0.63, 127 blue-dominant water pixels, an edit changes 1/5 sections — exactly the refreshed one, foundation Valid at +4 m) + windowed --play-caves / make p3d-caves (146 frames p50 0.43 ms): cave-interior capture human-inspected PASS (enclosing faceted stone, no sky leak, no seams), river before/after a 2x2 6 m raise dam with visible dam + ~1% image diff + LOCAL EDIT rows (4 dirty patches, 1/5 sections, refresh 9 us, water remesh 6 us). 9/9 visual gates green. REAL BUGS the proofs caught and fixed: a SurfaceRegion is a 48 m 3x3-PATCH window not a 3x3-region — build wrote 0.0 where refresh used the generator fallback, letting fake height changes hide (now both use the generator fallback and edit windows sit on the strip; the windowed proof caught it as an EMPTY dirty set); Raise STACKS on shared corner nodes (a 4x4 berm was a 24 m spike — the sky probe caught it); change-counting must compare heights OR the water line (an off-minimum raise moves heights but not the min). Previous: NWR-004 — ordinary natural terrain is now the SURFACE path: pc3d_render::surface_stream streams NWR-003 patches under the R3DV-006 laws (rings/bounded queue/per-frame caps/GPU budget/frustum) with ring LOD (17x17->9x9) and 2 m SKIRTS making LOD boundaries seam-safe without stitching; explicit TerrainPolicy keeps the legacy cube stream as fallback with all 9 gates green; collision served only from the full ring (refuses outside, within 1 m of the generator), edits feed collision. Proofs: 4 streamer tests + GPU vista (419 bounded frames, 1252 patches incl. the 1140-patch horizon ring) + windowed --play-surface-stream (251 frames p50 9.91 ms / avg 103 fps), human-inspected PASS — faceted hills to the skyline, no ring holes. Distance-gated eviction fixed the saturating-budget churn the teleport test caught. Previous: NWR-003 — the first real break from cube terrain, isolated to a 3x3 region: 17x17 boundary-grid patches with SEAM-SHARED edges (identical seam vertices test-asserted), authoritative heights/materials, sparse delta edits (Raise/Lower/Level) dirtying exactly patch+border-neighbors with a version table proving bounded remesh, collision from the SAME grid (bilinear height within facets, 3x3-stencil slope walkability that catches cliffs a cell-only check misses), compact delta save round-trip with corruption refusal. GPU+windowed proofs with cube-vs-surface discriminators (luminance bands, row-delta distribution vs banded flats, control-diff ground, edit-visible plateau); before-capture human-inspected PASS: faceted rolling natural hills, NOT cubes. A CW-winding bug that culling-invisible the terrain was caught by the pixel dump. Previous: NWR-002 — the renderer loads real glTF. THREE original assets ship as reproducible artifacts: prop.tree_ash (crooked trunk, 3 branch tiers, leaf clusters, roots — 1666/1800 tris), prop.rock_granite (fracture seam + moss — 98/600), module.house_croft (plinth/timber/thatch roof/chimney + 4 named sockets — 38/2500). Blender is NOT installed — the pack's alternative path: repository-owned Rust generator (tools/assetgen) writes valid glTF 2.0 GLBs deterministically (byte-identical regen). pc3d_assets::v2 validates the full schema-v2 + the factory-honesty law (non-planned status needs the .glb on disk; 15-case rejection matrix). pc3d_render::glb loads with named errors, LOD selection + coarsest fallback, sockets. Windowed proof --play-assets/make p3d-assets-window: 4 placements incl. two tree LODs in one frame, control-diff presence 0.22/0.33/0.34, foliage color check, 4 draws/1725 tris, p50 9.58 ms; PNG human-inspected PASS. The NWR-001 guardrail rows moved honestly (integrated + files on disk; inventory_is_truthful green). Previous: NWR-001 (over the completed R3DV-001..012 visual reset) — the rebuild baseline is secured: verification battery green (356/356 pc3d, 9/9 visual gates, smoke OK), the machine-readable capability_inventory.json records all 12 renderer paths honestly (module/representation/maturity/rebuild-target/proof; asset batch all planned, ZERO glb files on disk), and the new pc3d_render::inventory guardrail FAILS the suite on any false claim (sabotage-tested both directions — a coordinated pack+inventory shipped lie still fails on the missing disk file). Previous: R3DV-012 (the final task) — make p3d-visual-gates is the standing 9-gate regression battery: windowed-3d-axes, terrain-scenes, stream-walk, river-water, castle-city, npc-cast, quality-tiers, vertical-slice, asset-manifest — every windowed proof re-run with its semantic pixel assertions, loud failure, full console report to shots/gates_report.txt. Audit run 2026-09-07: 9/9 GATES PASS. VISUAL-GATES-REPORT.md holds the determination: per-gate frame records (all rendered gates >= 92 fps avg on the host iGPU, worst p95 21.6 ms; streaming steady-state ~1.1 ms/frame; tiers 42/80/102 patches at 2831/5468/6908 KB; showcase search 2.4 s), the human review (every gate PNG inspected in this audit and each task's original pass), and seven honest known limits. THE RESET CLOSED: from deterministic simulation + CLI PNGs to — windowed wgpu renderer, first-person walking with collision, terrain/caves/water/city/NPCs bound to the authoritative simulation, host-command building, quality tiers, save/reload, a walkable vertical slice on showcase seed 22, and a regression battery to keep it honest. Suites 356/356 pc3d + 474/474 root."
next_task: "OWNER FIXES LANDED (THE MOUSE: look writes the player body — proven to persist across live frames, re-verified from the DMG; THE STAMINA: sprint resource — walking free, sprint drains 0.22/s with exhaustion lockout; Shift sprint + Space jump wired). Next: the owner's manual play pass of the new UI (make p3d-rebuild-live / the DMG): PLAY from the title screen, walk, build with each hotbar material, save/load/delete through the menus, adjust settings mid-game, pause/resume, quit explicitly — the OWNER GATE before calling the slice playable. After that: hotbar items beyond materials (tools), damage-driven health, XP beyond first-build, audio placeholders, rebinding UI, per-world save thumbnails."
build: GREEN
tests: "p3d 457/457 (pc3d_render 153 incl. 26 UI layout/paint/input tests + app escape/modal/date/seed laws; +30 over loop 412), root 474/474, 10/10 visual gates (ui-states gate added), UI harness pixel checks PASS, inspector replay ok:true, no-args alive 12s on the title screen, DMG route proof PASS from the mounted read-only volume"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/ui_title_1280x720.png (human-inspected PASS: ember logo over backing band, five labeled buttons, no stray debug text; all 11 ui_* scenes inspected — docs/POORCRAFT-3D/GLM-UI-REWORK-PROOF.md)
blockers: "none technical — the owner's in-person play pass is the remaining gate"
