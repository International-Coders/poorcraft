# STATE
loop_count: 433
current_milestone: WT-009 LANDED: per-slot variant instancing — each flora slot picks its variant from the 300-GLB batch by a pure coordinate hash, meshes lazy-load on first sight, buckets split per (kind, LOD, variant): the wilderness vista draws 59 variant buckets of 62
last_done="WT-009 variant instancing (loop 433): the 300-variant batch draws in the wild. (1) THE PICK: variant_of(kind, slot, count) — FNV over slot coords + a stable per-kind tag, deterministic everywhere (cosmetic diversity from the world's own coordinates; deliberately seed-independent so a position keeps its variant across builds). (2) THE LOAD: KindGpu counts its variants on disk at startup and lazily loads a variant's LOD meshes on first bucket use (missing files negative-cache to the canonical base). (3) THE DRAW: cache entries carry the slot's variant; buckets key (kind, lod, variant); lit + shadow draws bind variant meshes when present; FloraStats gained variant_buckets. EVIDENCE: make p3d-wilderness — vista: buckets 62, instances 314, VARIANT BUCKETS 59 (95% of buckets are distinct variant meshes); capture band 882 distinct colors; laws: pick determinism + diversity (400 slots -> >=12 distinct per kind) + no kind aliasing; the 300-GLB consumer law stands; suites green. DEFERRED: seed-independent picks (deliberate), per-kind shared collision envelope, no generated far-LODs. Previous: WT-008 data extraction (432), playtest matrix (431). The WT implement-in-code walk is COMPLETE through WT-009.""
next_task: "WT-009 LANDED (loop 433) — the WT implement-in-code walk (WT-001..009) is COMPLETE. Remaining follow-ups as they surface: quest UI (journal + giver markers on the proven pc3d_world::quest authority), hotbar tools beyond materials, damage-driven health, audio placeholders, the loopback MCP server when wanted. THE OWNER PLAY PASS of the stamped DMG remains THE standing gate — everything the queue asked for is live: seed preview, enterable houses, talkable villagers, the chest->pick->harvest->forge->bar progression, the observatory, wireframes/overlays, GPU markers, data extraction, and now the variant-rich wild."
build: GREEN
tests: "p3d-wilderness: 59/62 variant buckets drawn + the standing consumer/proof gates; variant pick laws green; full p3d + root suites green"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/windowed_wild_vista.png (WT-009: the vista now draws 59 distinct variant meshes of the 300-batch — 882 distinct colors in the treeline band)
blockers: "none; unrelated dirty render/app/asset-capture files were left untouched for their owning work"
