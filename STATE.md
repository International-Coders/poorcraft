# STATE
loop_count: 424
current_milestone: WT-003 IN CODE (slices 1/2/5-partial/7): the game observatory — evidence bundles (18 fields + content digest), the 20-field runtime state export (null-with-reason), 4 available routes PASS with real captures + 4 honest UNAVAILABLE verdicts with reasons, and the comparator (pass/fail/inconclusive) proven between two independent real runs — via --observe / --compare-evidence / make p3d-observe
last_done="WT-003 (loop 424), observatory slices 1+2+5-partial+7. THE EVIDENCE LAYER: pc3d_assets::observatory embeds+parses the three WT-003 contracts (routes/bundle-schema/mcp-tools) with a law test; pc3d_render::observe ships the route table (title_mouse/new_world_seed/escape_pause/asset_inspect AVAILABLE — driven through REAL reducer paths incl. the WT-001 seed preview and Escape pause; house_entry/npc_talk/forge_use/gpu_markers honestly UNAVAILABLE with named reasons pointing at their slices), runtime_state_json (20 fields per runtime_state_export.schema: player/UI/world/visible-assets/interactions/perf live; npcs/machines/gpu_markers null-WITH-REASON per the schema law), bundle_json (18 fields per evidence_bundle.schema) with stamp_digest (FNV over content — test-proven that changing any field changes the digest), and compare_bundles (slice 7: readable/same-route/paths-exist-per-own-dir/screenshots-decode-nonblank/same-seed; differing seed => INCONCLUSIVE not fail; blank or missing => FAIL). CLI: --observe <route|all> (per-route re-exec — the winit one-loop law, the deck-bench lesson) writing bundle dirs; --compare-evidence. EVIDENCE: --observe all: 4/4 PASS (title_mouse 2 captures p50 6.4ms, new_world_seed, escape_pause, asset_inspect + the 7 WT-002 sidecars written into the bundle) + 4 UNAVAILABLE-with-reason; --compare-evidence on TWO INDEPENDENT real route_title_mouse runs: PASS, 7/7 checks; tests: observatory 1/1, observe 3/3 (incl. blank-fail + seed-inconclusive on constructed bundles); full suites re-run green. DEFERRED: slice 3 wireframe/anchor-overlay captures (renderer mode work, paired with WT-002 slice 3); the four gameplay routes flip UNAVAILABLE->PASS when their interaction slices land; slice 6 GPU markers (WT-007 slice 1). Previous: WT-002 semantic factory (loop 423), WT-001 seed preview (422)."
next_task: "WT-003 core LANDED (loop 424). Next: the visual evidence slices — WT-002 slice 3 + WT-003 slice 3 together (beauty/wireframe/anchor-overlay captures with pixel checks: a wireframe render mode + anchor marker overlay through the renderer), which also completes both packs' acceptance rows; then WT-007 slice 1 (GPU pass markers — flips route_gpu_markers to PASS). Keep the owner play-pass gate for the stamped DMG as the separate manual gate."
build: GREEN
tests: "observatory contracts 1/1, observe 3/3, --observe all 4/4 PASS + 4 honest UNAVAILABLE, --compare-evidence PASS on two independent real bundles (7/7 checks), full p3d + root suites green"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/observatory/route_new_world_seed/beauty_new_world.png (the observatory route capture: the New World screen with the live seed preview, bundled with runtime_state.json + perf.json + verdict.json + bundle.json digest)
blockers: "none for loop 418"
