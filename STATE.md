# STATE
loop_count: 451
current_milestone: THE CROWD YIELDS IN THE WINDOW: the staged-yield crowd route — two settlement bodies walk one open row head-on through the LIVE slice's own crowd tick, one walks through, the other sidesteps around it, nobody shares a cell on any of the 70 audited frames, both arrive at their declared sites, and the windowed captures frame the yield — the 448 law's last route deferral closed
last_done="The crowd yields in the window (loop 451, POORCRAFT 3D): closed STATE's next_task item (1) — the STAGED-YIELD CROWD ROUTE the 448 law has been waiting for (unit-lawed + render-lawed, but no windowed route framed two NPCs yielding; main.rs uncontended). (1) THE STAGE (renderer.rs, route-proof hook crowd_stage_head_on_near + readers crowd_cell/crowd_work_site): rewrites two cast members into head-on walkers on ONE fully walkable row near a requested center — real nav paths the Work-phase schedule keeps — and parks the rest at their homes (Working) so the staged pair is the whole moving cast; a row is admitted only when BOTH nav paths are STRAIGHT along it (with every row cell walkable the straight path IS the nav path, so an off-row body cell later is yield evidence, never a detour) AND the row plus its ±1 sidestep band touches no settlement collision cell (the nav is terrain-only — the first staged row at the plaza's own z walked through house footprints; the collision-band refusal moved the route to the open band) AND the parked home sits clear of the band; rows are tried nearest the requested center first inside the nav-patch interior (a plaza can sit on a patch corner — every plaza-relative ±span crossed the border and answered None). (2) THE ROUTE (make p3d-crowd, route_crowd_yield, registered in observe.rs): the live slice's own crowd_tick(0.35, 1) then runs the real law every frame — cast 0 walks through, cast 1 is refused at gap 2 and SIDESTEPS (tick 4: A holds the mid cell, B stands the sidestep beside it), re-paths around, and both arrive Working at their DECLARED sites; a per-frame ui_script audit (frames 43..=112, 70 recorded) ORs the flags: NO shared cell on any audited frame, the off-row yield seen, read failures poison the run; the arrival step re-reads crowd_work_site both ways and the parked cell unchanged. THE VANTAGE: the body teleports 5 m south of the row's middle facing north — both row ENDS inside the ~71% half-width, the walkers large in frame by the yield tick; captures crowd_staged (42, closing), crowd_yield (43, the sidestep — two bodies adjacent mid-row), crowd_pass (47, separated on opposite sides), crowd_arrived (130, both at their ends). PASS x2 identical + comparator PASS; the four captures INSPECTED (in debug AND release runs). (3) EN-ROUTE DISCOVERIES, both fixed before committing: the nav-blind first row (385,129)–(393,129) crossed the settlement's house band — the hook's new collision-band refusal now rejects any row touching a footprint (z 129 was `##.##.##..##....` on the patch map; the route stages at (387,132)–(395,132) in the open); and the FIRST windowed captures framed the wrong things twice (vantage landed at spawn — capture frames moved behind the teleport — then a kit stall filled the lens at the row-end vantage; the south-of-row composition at 5 m frames both ends and the yield). (4) PRIOR-PROOF FIX (the people harness, same family as 450's walkoff re-timing): p3d-people's motion check compared frames 30↔50 whose stride phases follow the WALL CLOCK — at this week's ~22.5 ms frame pace the phases aliased and the metric read 0.0016 (FAIL) then 0.0020 (PASS, 0.3 over the bar) on IDENTICAL code, vs 448's 0.0103 — the check sat at its bar's edge every run. The harness now freezes the pose clock (set_water_time, already honored by prepare_frame) at two KNOWN phases before the compared captures: the metric reads 0.0065 / 0.0063 across runs — 3x the bar and scheduling-independent; the compared frames differ by the rig's pose, not by luck. (5) EVIDENCE: p3d workspace 676 green / 0 failed (pc3d_render 221 = 220 + 1 staged-yield law [the_staged_head_on_pair_yields_through_the_live_law: the hook's pair through the render-facing advance never shares a cell, both arrive declared, the parked member never moves, a body provably leaves the row]; pc3d_world 265 unchanged); make p3d-crowd PASS x2 + comparator PASS (captures inspected); make p3d-playtest x2 + comparator PASS, bundle digest UNCHANGED 05c46411869a857c; make p3d-people PASS x2 (12 NPCs, 7 draws, 92 instances, frozen-stride motion 0.65%/0.63%); p3d-smoke OK (digest dd019eca900f5a61 unchanged); p3d-assets OK; idle-upgrade-check PASS. (6) PERF: no claim, honestly — the route adds work only while it runs (staging scan = bounded nav A* calls; the per-frame audit = two HashMap-backed cell reads); the live walk/stream/crowd paths are untouched; host shared (windowed routes p50 ~22-26 ms, the local norm), no bench per the 445-450 precedent; the quiet-host re-read stays queued (six loops). LORE: canon touched: none — a proof route over the existing crowd law on unnamed settlement bodies; the yield is LOCAL perception (a body yields to the body in front of it), never omniscient pathing, per the personhood law; no faction, place, event, term, NPC, item, or spell data; no identity assigned. World expression: people in Valdenmoor share the ground — the windowed game now shows a body step aside for another instead of ghosting through. Migration: none (in-memory staging inside one route run; no persisted field, save format, or proof schema change). Deferred honestly: the inventory echo of the dig verb (next_task item 1); Space jump-off from a hang; the lethal plaza-recovery branch route; the quiet-host deck-bench re-read + the bench contention guard; is_water_layer/CTM-strip audit; guardian chronicle re-fire; multiplayer routing of terrain edits; geode pairing (447's keepers are root-workspace lore)."
next_task: "Carry the list (p3d): (1) The inventory ECHO of the dig verb: a small carried-stock readout (the hotbar's empty slots 6-9 or the HUD stock lines) so the dug take is visible as a number, closing the verb's loop with the existing forge/deliver economy — the toast names the take today, can_fit/add/outcome are unit-lawed, but no UI shows the count. (2) The lethal plaza-recovery branch route (the fall law's 0.5-health plaza recovery has no windowed route). Carried deferrals: Space jump-off from a hang (unit-lawed only); the quiet-host deck-bench re-read (queued by 446..451) + the bench contention guard; is_water_layer/CTM-strip audit; guardian chronicle re-fire; multiplayer routing of terrain edits; geode pairing (447's keepers are root-workspace lore)."
build: GREEN
tests: "p3d workspace 676 green / 0 failed (pc3d_render 221 = 220 + 1 staged-yield law; pc3d_world 265 unchanged); make p3d-crowd PASS x2 + comparator PASS (captures inspected in debug AND release runs); make p3d-playtest x2 + comparator PASS, bundle digest UNCHANGED 05c46411869a857c; make p3d-people PASS x2 (frozen-stride motion 0.65%/0.63% vs the 0.002 bar — the wall-clock aliasing fixed; 12 NPCs, 7 draws, 92 instances); p3d-smoke OK (digest dd019eca900f5a61 unchanged); p3d-assets OK; idle-upgrade-check PASS; no bench recorded (shared host — documented above)"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/crowd-a/route_crowd_yield/crowd_yield.png (two bodies adjacent mid-row — the sidestep beside the walker who held the line — from this loop's green make p3d-crowd run)
blockers: "none; a proof route over the existing live crowd law — no save format, worldgen, or asset change"
