# STATE
loop_count: 465
current_milestone: THE YIELD IS THE SERVER'S TO GIVE: multiplayer digs pay through the server — the dig only CLAIMS its hand on the wire (SetBlock.mine: the held item id, bare hands included; places and simulation edits claim nothing), the server evaluates its own copy of the harvest law against the block it actually had, and grants the canonical drop back to the editor ALONE (ItemGrant) — so a rejected op, a place, a sim edit, a re-sent duplicate, or a peer's earlier dig of the same block can never pay a phantom item (the 462 corrective-echo gap closed: the echo reverts the BLOCK, and the yield was never optimistically taken)
last_done="The yield is the server's to give — server-side dig-yield authority over real UDP (loop 465, root LOREFORGE): closed STATE's next_task item (1)'s higher-severity half (the 462 deferral 'server-side inventory authority — the corrective echo reverts the BLOCK, not an optimistic item take'; chosen over the windowed two-client route per the priority ladder — an authority gap ranks above a missing proof). THE WIRE (lf_protocol, v5): SetBlock gains `mine: Option<MineClaim>` — Some iff the edit is a player MINED dig, carrying the honest held item id (None inside = a bare hand, which still claims: a bare-handed dig of a no-tool block pays online exactly as offline); places/machine/fluid/falling/console/server-mirror edits claim nothing; new ServerMessage::ItemGrant { items } — the canonical yield, editor ALONE; PROTOCOL_VERSION 4 -> 5 (matched binaries; the existing version gate rejects mismatched peers). THE SERVER (lf_server + lf_game dep): on an accepted mine the server reads what the block WAS (before its canonical world changes), gates the claim through mining::tool_satisfies and pays items::block_drop — the SAME law the client plays, one source; the grant rides to the editor only, never to peers. THE CLIENT (lf_client): the host funnel computes the claim (net::mine_claim_for) and is THE ONE BROADCASTER (source law); break_block_drops — the ONE path every canonical block yield spawns through — spawns only OFFLINE (net.is_none(); source law), online the yield ARRIVES via ItemGrant and lands in the pack with the trade-escrow overflow-spill pattern; the scaffold column now yields through break_block_drops (its direct spawn was ungated) — per-cell server grants match singleplayer; felled-tree cells are the TIMBER'S, not the hand's (EditKind::Mine -> Falling: singleplayer never paid them as drops, so online they must claim nothing or the server would grant a log per trunk cell); the leaves-apple bonus is offline flavor (the server owns no RNG yet); the keeper's waking + the twin's song stay unconditional (reactions, not items). EN-ROUTE WIRE WART FIXED: the mine-break path sent its dig a SECOND time after the funnel had already broadcast (every dig double on the wire + double-entered in the server's edit history) — removed and pinned. 7 NEW LAWS (root 490 -> 497 in-workspace + xtask 12 = 509 total; lf_protocol 5 -> 6 tests, lf_server 4 -> 7, lf_client 101 -> 104): the claim/grant round-trip law; the yield-grant wire law (mine pays the editor ALONE, a place never pays, the peer never sees a grant); the harvest-gate-server-side wire law (iron ore: wooden pick pays nothing, stone pick pays, bare hand fails the gate); the pays-once + rejected-never-pays wire law (re-sent dig into already-air, unknown-id op: corrective echo only); the mine-claim unit law (only Mine claims, the claim is the honest hand); the one-broadcaster source law; the no-optimistic-take source law (gated break_block_drops + scaffold routing + ItemGrant arm). REGRESSION: cargo test --workspace 509 green / 0 failed + cargo test -p xtask 12 green (already counted; = 502 + 7); make smoke OK; FULL vistest battery 110 scenes, exit-0, every committed vistest PNG byte-identical on disk (singleplayer renders the same paths — the 462/461 precedent made pixel-proof this loop); runtimes refreshed — dist/loreforge-macos.dmg 8.8 MB (hdiutil VALID) + linux tarball 8.4 MB + .app binary 20 MB + server, fresh 16:19-16:20 (gitignored — the loop-446 convention); Windows exe honestly skipped (mingw absent); POORCRAFT 3D untouched; the six windowed_wild_*.png dirties remain deliberately NOT staged (an earlier session's p3d-wilderness run). PERF: a small net WIN on the multiplayer wire — one redundant SetBlock per dig removed (halves mine-edit traffic and the server's edit-history growth); the grant adds one small message per ACCEPTED, tool-legal mine; zero singleplayer cost (all new paths sit behind net.is_some()/is_none()). LORE: canon touched: none — multiplayer economy plumbing; the granted items are the existing canonical drops (anima_crystal's Covenant world-source unchanged); no faction, place, event, term, NPC, item, or spell data changed; no new canon text. Canon preserved: the chronicle as the player-authored history (the twin song and keeper waking are reactions, not grants); Anima as a material energetic property; no identity assigned. World expression: in shared Valdenmoor a mined vein pays once, to the miner whose swing the realm accepted — the harvest law, not each adventurer's say-so. Migration: PROTOCOL_VERSION bump only (matched client+server; old peers rejected by the existing gate) — no save format, no block/item change, GENERATOR_VERSION unchanged, ClientSave untouched. Deferred honestly: the server does not yet HOLD canonical inventories (crafting/smelting/trade sourcing stay client-held; the grant covers mined yields — server inventory state rides the integrated-server tier); the leaves-apple bonus is offline-only (no server RNG); the timber landing plan's blocked-cell drops and container spills (chest contents) stay client-local — the sim/block-entity sync tier; mob drops client-local (pre-existing); the carried list — a windowed two-client route (the GPU-side end of the wire laws; GameState owns a real window+surface, so the route needs a headless driver slice of its own); hardcoded connect name 'smith' (audit note)."
next_task: "Carry the list (root): (1) The windowed two-client route — a second client SEES a dig land (the GPU-side end of the wire laws): GameState owns a real window+wgpu surface, so the honest route needs a headless client driver (drive GameState-free logic or a surfaceless render path) — scope that driver first, then the route: server + client A digs + client B renders the hole, pixel-gated. (2) Server-side inventory STATE (the next tier after 465's grants): the server holds per-player canonical inventories so crafting/smelting/trade sourcing stop being client-held. Check STATE/build first: prefer the higher-severity item per the priority ladder."
build: GREEN
tests: "cargo test --workspace 509 green / 0 failed (= loop 464's 502 + 7 new laws: lf_protocol 5 -> 6 tests, lf_server 4 -> 7, lf_client 101 -> 104) with cargo test -p xtask 12 green included; make smoke OK; vistest battery 110 scenes [ok] / 0 FAIL, exit-0, all committed scene PNGs byte-identical"
last_screenshot: "none new (nothing visual changed — the 458/459/461 precedent); the battery re-rendered all 110 committed vistest PNGs byte-identical, which IS the visual proof of singleplayer parity; newest visual scene remains shots/vistest_resonance_compass_hud.png (loop 464)"
blockers: "none; the six windowed_wild_*.png dirties are NOT this loop's and remain unstaged"
