# STATE
loop_count: 347
current_milestone: Complete — hitboxes/walls/wheels/castle-siting user bug hunt
last_done: "loop 347 hitbox-and-spawn pass: (1) LAVENDER/SUNFLOWER were missing from the per-id solid/opaque exclusion lists, making whole flower biomes invisible solid full cubes that culled the ground faces under them — is_solid/is_opaque now exclude via !is_plant(id); (2) real pick shapes: registry::pick_boxes (plant inset box, torch stick, lantern cage, slab/stair shapes) drives a new shape-aware raycast_voxel_boxes and a per-box selection wireframe, so crosshair + outline match the visible block instead of the full cell; (3) animals walk through walls fixed — MobEntity::physics_step does axis-separated AABB collision through the player's own box resolver (extracted as pub(crate) box_intersects_solid) with substepping, landing clamps, hop-assisted 1-block step-up and a wedge pop-up; the dragon flight brain's position proposals are clamped against terrain; (4) the creative 2-blocks/sec removal cap was mob_in_crosshair having no occlusion test — any mob near the look line stole LMB into the 0.5s attack cooldown; new crosshair_mob filters candidates through has_line_of_sight; (5) the wheel consumed only signum() per frame, discarded fast-flick notches and did a window.set_title round trip per step — now every notch lands the same frame before the UI draws, remainder kept for trackpads, title refresh left to the HUD tick; (6) new hotbar_caption line above the hotbar shows the looked-at block's name (item name while a switch fades); (7) castle spawn logic rebuilt: kingdom_chunk_ok validates a dense 6x6 grid over the whole footprint (any wet/alpine cell refuses), candidates keep 2 chunks off region borders, sites stay 160 blocks clear of world spawn, build_kingdom_citadel carves the hillside above base so walls/gates are never buried, citadel chunks skip tree/cover passes, GENERATOR_VERSION bumped to 6. 399 tests (+12), 93/93 vistest, smoke OK, runtimes rebuilt."
next_task: "First-minute onboarding pass: contextual movement/mining/crafting prompts plus a pinned starter objective, dismissible and persisted, with compact HUD and small-window vistest proofs (carried over from loop 344 planning)."
build: GREEN
tests: 399 passed / 0 failed (loop 347)
last_screenshot: shots/vistest_kingdom_citadel.png
blockers: "none"
