# STATE
loop_count: 470
current_milestone: THE BITE IS THE LEDGER'S TO FEED: server-side eating over real UDP (protocol v10) — the last client-side consumption tier closes, so every player-facing consumption of the played loop is now server-computed. While a SURVIVAL session is connected, a food click is only a REQUEST: the client applies nothing (the PackSync claim stays exact in both UDP orderings), the server gates the bite twice — the realm's own catalog decides what food is (lf_game::items::item_def; what the realm does not call food, it will not feed) and the canonical LEDGER must pay (exactly `count` leaves the pack) — and the EatVerdict answers the eater ALONE. On grant the local pack consumes one of the named food from wherever the ledger's twin holds it (the click's hotbar slot may have scrolled away while the verdict flew) and the hunger feeds, clamped; a refusal only says why. A replayed req_id (a duplicated datagram) is answered with a no-op refusal — a bite pays once. Creative bites locally (infinite by its own law — the placement law's twin); offline the integrated host is the same authority in process, byte-equal to the shipped law. A lost verdict errs safe: the hunger never applies and the pack's re-claim restores the ledger's remainder — a bite can be lost in flight, never fabricated.
last_done="The bite is the ledger's to feed — server-side eating over real UDP (loop 470, root LOREFORGE): closed STATE's next_task item (2) — eating, the last client-side consumption tier of the same gated-request shape (smelting DONE 469), chosen over the windowed two-client route per the priority ladder (an authority gap above a missing proof, the 465-469 precedent). After this loop every player-facing consumption of the played loop — mining, trading, crafting, placement, smelting, eating — is server-computed against the canonical ledger. THE WIRE (lf_protocol, v10): EatRequest { req_id, item, count } and Eat(EatVerdict { req_id, granted, reason }) to the eater ALONE — THE VERDICT CARRIES NO ITEM DELTA because the client applied nothing before it; PROTOCOL_VERSION 9 -> 10 (matched binaries; the existing gate rejects mismatched peers). THE SERVER (lf_server): THE EAT-OP LAW (the shared pure fn eat_op, unit-lawed): the FOOD GATE (the realm's catalog decides what food is — a log, a pickaxe, an unknown stew, a zero-count bite all refuse by name) + the LEDGER (count_of must cover the bite; remove_count pays exactly — the pre-check guarantees the pay). THE REPLAY WINDOW (the craft pays-once law's eating twin, own per-kind seen window); unknown senders ignored; peers hear nothing about another player's appetite. THE CLIENT (lf_client): the right-click food arm is the ONE eat site — online survival the click SENDS and records a bounded in-flight bite (EAT_IN_FLIGHT_CAP 64), offline and creative keep the shipped local bite byte-equal; the Eat arm is a thin delegate to the ONE resolver (resolve_eat_verdict): a granted bite consumes exactly one of the food by item id (never the held slot directly — the ledger paid \"one apple\", not \"that slot\") + hunger clamped + the eat sfx, a refusal hints \"cannot eat: …\" and moves nothing, a verdict for a dropped id is ignored (at-most-once, never fabricated). 6 NEW LAWS (root 545 -> 551; lf_protocol 8 -> 9, lf_server 30 -> 34, lf_client 113 -> 114): the eat round-trip codec law; the eat-op unit law (food pays exactly; non-food/unknown/zero-count/short-ledger refuse by name and move nothing); the funded-bite wire law (the ledger pays, the trade gate proves the remainder, the peer hears nothing, an unpaid bite refuses by name and never conjures an apple); the food-gate wire law (a log, a pickaxe, an unknown item, a zero bite all refuse; the ledger never moves — the offer gate passes everything); the bite-pays-once replay wire law (the same req_id twice = one bite's charge); the online-bites source law (the one sender guarded on a live session + survival, offline keeps `if !sent`; the arm a thin delegate; the resolver consumes exactly one by item and never consume_selected). REGRESSION: cargo test --workspace 551 green / 0 failed (xtask's 12 included = loop 469's 545 + 6); make smoke OK; FULL vistest battery 110 scenes [ok] / 0 FAIL exit-0 across TWO runs with every committed PNG byte-identical (md5 462b2318ed4c98d9a882266cd81fbd7d before == after both runs) — singleplayer render paths pixel-proven unchanged; --features steam lib + examples compile clean with the v10 wire; POORCRAFT 3D builds untouched; runtimes refreshed (dist/ dmg + linux tarball + .app + server); Windows exe honestly skipped (mingw absent); the six windowed_wild_*.png dirties remain deliberately NOT staged. PERF: not applicable — one small request datagram per bite and one small verdict back; the server adds one catalog lookup + one count_of/remove_count per request; zero singleplayer cost (offline branch byte-equal). LORE: canon touched: none — multiplayer economy plumbing (the 465-469 precedent); the food gate reads the existing item catalog; no faction, place, event, term, NPC, item, or spell data changed; no new canon text (rejection reasons are UI hints). Canon preserved: the chronicle as the player-authored history; Anima as a material energetic property (a bite is mundane sustenance the realm's ledger can account — no miracle, no identity assigned); no identity assigned. World expression: in shared Valdenmoor the realm's ledger knows what each adventurer truly ate — a bite is paid from the pack the ledger knows, and every refusal says why. Migration: PROTOCOL_VERSION bump only (matched client+server; old peers rejected by the existing gate) — no save format, no block/item change, GENERATOR_VERSION unchanged, ClientSave untouched (eating is stateless per request; no new server-persisted state)."
next_task: "Carry the list (root): (1) The windowed two-client route — a second client SEES a dig land (the GPU-side end of the wire laws): GameState owns a real window+wgpu surface, so the honest route needs a headless client driver (drive GameState-free logic or a surfaceless render path) — scope that driver first, then the route: server + client A digs + client B renders the hole, pixel-gated. With the consumption tiers closed (465-470), this is now the top carried item and the multiplayer authority program's remaining proof. (2) The lying-PackSync bootstrap tier — a modified client's claim can re-seed the ledger (named at 466, unchanged); full closure = server-side mode authority + spawn inventory. Check STATE/build first: prefer the higher-severity item per the priority ladder."
build: GREEN
tests: "cargo test --workspace 551 green / 0 failed (= loop 469's 545 + 6 new laws: lf_protocol 8 -> 9, lf_server 30 -> 34, lf_client 113 -> 114); make smoke OK; vistest battery 110 scenes exit-0 twice, all committed scene PNGs byte-identical (md5 462b2318ed4c98d9a882266cd81fbd7d)"
last_screenshot: "none new (nothing visual changed — the 462/464-469 precedent; the eat click's pixels are identical, only its online survival branch is gated); the newest visual scene remains shots/vistest_resonance_compass_hud.png (loop 464)"
blockers: "none; the six windowed_wild_*.png dirties are NOT this loop's and remain unstaged"
