# STATE
loop_count: 414
current_milestone: BUILD-IDENTITY FIX (the 'you are not really fixing it' pass): the diagonal-cut fix WAS in every shipped DMG since f5d75b6 — but the owner was playing a STALE DMG volume mounted at Sep 9 15:03, ONE MINUTE before the 15:04 fix commit, and macOS kept it mounted under the same volume name so the fresh builds' DMG mounted as 'POORCRAFT 3D 1'. Every build now identifies itself (title screen shows BUILD <version> <git-hash>; DMG volume is POORCRAFT3D-<hash>), the stale volume was ejected, and the ui-shots gate gained the unaligned-width legs + a row-shear law so the diagonal-cut bug class can never hide behind an accidentally-aligned proof width again
last_done="BUILD-IDENTITY + SHEAR-LAW (loop 414, owner order: 'play the game again, test the screen, the menus are still cut diagonally'). REPRODUCTION ON THE REAL SCREEN: launched the current release binary windowed and photographed the composited display at widths 1000/1501/1512/1777/1024 (1501/1777/1000 have NON-256-aligned row pitch — the owner failure mode) across title/focus/new-world/Esc/gameplay states: median row drift +0.0px everywhere — the shipped binary is shear-free; the DMG binary is byte-identical (sha256 0f31c20b...). ROOT CAUSE of the owner still seeing the cut: /Volumes/POORCRAFT 3D was a stale mount from Sep 9 15:03 — the diagonal-cut fix committed at 15:04:20, ONE MINUTE LATER — and every rebuilt DMG reused the volume name 'POORCRAFT 3D', so fresh mounts collided as 'POORCRAFT 3D 1' while the owner double-clicked the old volume (app POORCRAFT3D.app, sha256 716d063a... = pre-fix). FIX (three layers): (1) both stale mounts ejected; (2) BUILDS ARE SELF-IDENTIFYING — ui::build_stamp() bakes PC3D_BUILD (git short hash; make p3d-dmg passes it) into the title subtitle '3D · OWNER ALPHA · BUILD <version> <hash>' and the DMG volume name is POORCRAFT3D-<hash>, so two builds can never collide or masquerade; PLAY.md gained a 'WHICH BUILD AM I RUNNING?' section; (3) THE GATE BLIND SPOT IS CLOSED — the original bug hid because all proof widths (1280/2560/1280x800) were accidentally 64-px aligned: --ui-shots now resizes to 1501x801 mid-run and captures the gameplay HUD + title there (13 captures), and verify_ui_captures runs a ROW-SHEAR LAW on every capture's composited readback (adjacent-row gradient cross-correlation: fail if median drift != 0 or one nonzero shift owns >25% of structured rows — tuned against the genuinely-noisy dark-terrain modal scene which scatters -2/-1/+1 at 12.7% max mode); new unit tests: title_canvas_has_no_row_shear_at_unaligned_widths (1501x801@1x + 3024x1964@2x) and build_stamp_never_empty_and_shown_on_title. EVIDENCE: make p3d-ui-shots 13/13 PASS (every capture 'no row shear', incl. ui_gameplay_hud_1501x801 + ui_title_1501x801); real-screen shear analyzer (Python adjacent-row cross-correlation) on the MOUNTED DMG binary at 1280 default + 1501 unaligned title/gameplay/pause: median +0.0px, frac 0.03-0.04; journey digest 7ab2295dafa0ec24 from the mounted volume; stamp 898f337 verified inside the mounted binary next to title_sub; DMG = poorcraft3d/dist3d/poorcraft3d-macos.dmg (volume POORCRAFT3D-898f337); screen proofs committed under poorcraft3d/apps/poorcraft3d/shots/screen-proofs/. p3d 580/580 (+2), root suite exit 0 (workspace untouched, standing 474). HONESTLY DEFERRED: Retina 2x real-screen verification ran through the harness readback only (this host's displays are 1x — the 3024x1964@2x law is covered by the canvas test + the build_dpi path); the shear law thresholds are calibrated to this content set. THE OWNER GATE STANDS: manual play pass of the DMG (now with a visible BUILD line so the running build is never in doubt). Previous: four-fold owner order (height map + terracing fix, biome tests, 300 variant assets, 108 city/npc/quest tests) — full history in DEVLOG.md/CHANGELOG.md."
next_task: "LANDED (loop 414): the stale-volume root cause + self-identifying builds (BUILD hash on the title screen, POORCRAFT3D-<hash> DMG volumes) + the row-shear law at unaligned widths (13-capture ui-shots gate). NEXT: the owner's manual play pass — open poorcraft3d/dist3d/poorcraft3d-macos.dmg (volume POORCRAFT3D-<hash>), confirm the title subtitle BUILD hash matches the volume name, then PLAY: walk, build with each hotbar material, save/load/delete through the menus, settings mid-game, pause/resume, explicit quit. If anything looks cut or wrong, check the BUILD line first — if the hash differs from the volume you opened, it is a stale mount (eject all POORCRAFT3D-* volumes and reopen). After the pass: per-slot variant instancing in the wild, quest UI, hotbar tools, damage-driven health, audio placeholders."
build: GREEN
tests: "p3d 580/580 (+2: unaligned-width canvas shear law, build-stamp law), root exit 0 (untouched, standing 474), ui-shots 13/13 incl. 1501x801 legs with the row-shear law on every capture, real-screen shear analyzer on the mounted DMG binary: median +0.0px at 1280/1501 across title/gameplay/pause, journey digest 7ab2295dafa0ec24 from the mounted volume"
last_screenshot: poorcraft3d/apps/poorcraft3d/shots/screen-proofs/p3d_mount_title1501.png (real-screen capture of the MOUNTED DMG binary at unaligned width 1501: median row drift +0.0px; the subtitle band with the BUILD hash is cropped in p3d_mount_buildline.png)
blockers: "none technical — the owner's in-person play pass of the stamped DMG is the remaining gate (now trivially verifiable: the title screen shows the BUILD hash)"
