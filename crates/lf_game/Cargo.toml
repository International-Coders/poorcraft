[package]
name = "lf_game"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
glam = { version = "0.30", features = ["serde"] }
lf_voxel = { path = "../lf_voxel" }

[dev-dependencies]
bincode = "1"
