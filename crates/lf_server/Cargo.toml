[package]
name = "lf_server"
version = "0.1.0"
edition = "2021"

[dependencies]
lf_protocol = { path = "../lf_protocol" }
lf_voxel = { path = "../lf_voxel" }
lf_worldgen = { path = "../lf_worldgen" }
serde = { version = "1.0", features = ["derive"] }
bincode = "1"

[dev-dependencies]
