[package]
name = "loreforge"
version = "0.1.0"
edition = "2021"

[dependencies]
lf_engine = { path = "../../crates/lf_engine" }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
