# POORCRAFT 3D Task Contract

## Identity

- Task ID: P3D-701
- Title: Valve-era computing — signals, relays/valves, programmable controllers, clear debugging
- Stage/gate: P3D-700 (advanced paths)
- Owner/model: owner directive; z.ai GLM session
- Date: 2026-09-06

## Goal

Valve-era logic gates (AND/OR/NOT/XOR), signal wires, and configurable
controllers exist as pure deterministic components on the D-012 valve
aesthetic. Clear debugging: gate state and wire signals are queryable.

## Scope

- `pc3d_world::valve_computing`:
  - `Signal(u8)` — 0 (off) or >0 (on, value = strength).
  - `GateKind` (And, Or, Not, Xor) with `evaluate(inputs) -> Signal`.
  - `LogicCircuit` — named gates + wires; `evaluate(input_signals) ->
    BTreeMap<gate_name, Signal>` (deterministic evaluation order).
  - `ValveController` — programmable: set input signals, read output.
- Tests: gate truth tables, circuit evaluation, determinism,
  multi-stage circuits.

## Done when

Tests green, suite green, root untouched, docs agree.
