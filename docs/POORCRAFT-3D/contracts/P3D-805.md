# POORCRAFT 3D Task Contract

## Identity

- Task ID: P3D-805
- Title: Complete beta player-journey automation
- Stage/gate: P3D-800 (integration & multiplayer)
- Owner/model: owner directive; z.ai GLM session
- Date: 2026-09-06

## Goal

One automated beta journey — spawn, gather, craft the first tool, mine,
craft up, cook and eat, build, power an industry chain, visit a
settlement, slay a dragon, complete onboarding — executed against the
integrated host and item authority, reporting a per-step pass/fail log
and a digest that is bit-identical on re-run. Journey automation
surfaces the progression gap it finds (no craftable first pick) and the
fix ships in the same job.

## Scope

- Progression fix (append-only): item 12 `wood_pick` (tier 1) and
  recipe 6 `wood×4 → wood_pick` — the craftable first tool.
- New `pc3d_world::journey`:
  - `run_journey(seed) -> JourneyReport` with named steps and per-step
    evidence; `--journey [seed]` CLI arm + make target.
- Tests: journey passes on multiple seeds, re-run determinism, the
  wooden pick is craftable and the tool gate opens.

## Done when

Tests green, suite green, root untouched, docs agree.
