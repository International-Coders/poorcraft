# POORCRAFT 3D Task Contract

## Identity

- Task ID: P3D-106
- Title: Biome/hydrology constraint tests — mountains, coasts, river corridors, sites, reproducible seed history
- Stage/gate: P3D-100 (world substrate), stage closer
- Owner/model: owner directive "finish ALL steps"; z.ai GLM session
- Date: 2026-09-05

## Goal

A first-class constraint suite proves the generated world satisfies every
geography rule across many seeds: mountain shapes, coastal transitions,
river-corridor wetness gradients, site placement on viable terrain, and
reproducible seed history (same seed → same biome at every point).

## Scope

### Must change

- `pc3d_world::constraints` — a consolidated constraint matrix that
  reuses the existing gen/hydro/proof APIs to verify, across N seeds ×
  M regions: mountain shape profiles (elevation range, defense decay),
  coastal transition bands, river-corridor wetness gradients, site
  viability (settlements on land near water), and seed-history
  reproducibility (same seed → same biome map hash).
- Tests: the constraint matrix itself is the test.

### Explicit non-goals

- No new generation features; this task tests what P3D-103 built.

## Done when

Constraint matrix green across the declared sweep, suite green, docs
agree.

## Handoff

- next task: P3D-201+ continues (P3D-100 stage fully closed).

## Required design check

- Improves: every story — the geography must be trustworthy.
- Connects: gen (P3D-103) ↔ hydro (P3D-301) ↔ settlements (P3D-407).
- Sees: a world where mountains are mountains and rivers are wet.
- Depends on: D-016, D-026.
