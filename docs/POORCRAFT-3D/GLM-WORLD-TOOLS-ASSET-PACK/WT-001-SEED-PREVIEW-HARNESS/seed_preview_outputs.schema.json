{
  "version": 1,
  "required_fields": [
    "seed_text",
    "resolved_seed",
    "world_type",
    "preview_size",
    "spawn",
    "biome_counts",
    "feature_hints",
    "valid",
    "warnings",
    "screenshot_path",
    "layout_path"
  ],
  "spawn_fields": ["x", "y", "z", "safe", "reason"],
  "feature_hint_fields": ["kind", "distance_m", "confidence", "placeholder"]
}
