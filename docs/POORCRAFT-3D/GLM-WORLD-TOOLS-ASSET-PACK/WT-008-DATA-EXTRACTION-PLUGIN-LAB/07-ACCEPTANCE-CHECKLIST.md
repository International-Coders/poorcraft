# Acceptance Checklist

WT-008 is done when:

- [ ] all plugin/export JSON contracts parse;
- [ ] parent world/tools manifest lists every WT-008 file;
- [ ] pc3d_assets guard parses every WT-008 JSON;
- [ ] exporter contracts cover scene, UI, asset, mesh, material, player, NPC,
      machine, seed preview, worldgen, perf, and evidence bundles;
- [ ] local-only safety constraints are explicit;
- [ ] sample good/bad mod packs are defined;
- [ ] bad packs must fail validation;
- [ ] telemetry is local-first and optional.
