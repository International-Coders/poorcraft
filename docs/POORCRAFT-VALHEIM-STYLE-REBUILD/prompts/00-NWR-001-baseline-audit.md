# NWR-001 — Baseline Audit and Guardrail Prompt

```text
Implement NWR-001 only from docs/POORCRAFT-VALHEIM-STYLE-REBUILD/contracts/rebuild_roadmap.json.

First inspect the actual code and current git state. Run the established P3D
unit, windowed visual-gate, and slice commands. Do not change terrain, assets,
rendering, world generation, or saves in this milestone.

Create an evidence-backed rebuild baseline: enumerate current real renderer
paths, which assets are primitive/flat-colour/procedural, all screenshot gates,
runtime commands, performance numbers, and data boundaries. Add a focused,
machine-readable capability inventory that later milestones can update. It must
make false "finished art" claims impossible. Link it from the rebuild pack.

If a pre-existing gate fails, diagnose and fix that regression before creating
new scope. Make a focused implementation-quality commit only if code/test work
is needed; do not make a documentation-only commit. Follow AGENTS.md for all
bookkeeping, runtime packaging, commit, and push rules. Stop after NWR-001 is
green and report exact evidence and any blocker.
```

