# Baker's Stock — generated community pack (see mods/README.md)
id = "baker_stock"
name = "Baker's Stock"
version = "1.0.0"
api_version = "1"
side = "both"
dependencies = ["core"]
permissions = ["world.read", "world.write"]
