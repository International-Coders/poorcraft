# Healer's Kit — generated community pack (see mods/README.md)
id = "healer_kit"
name = "Healer's Kit"
version = "1.0.0"
api_version = "1"
side = "both"
dependencies = ["core"]
permissions = ["world.read", "world.write"]
