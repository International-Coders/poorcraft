# Bamboo Grove — generated community pack (see mods/README.md)
id = "bamboo_grove"
name = "Bamboo Grove"
version = "1.0.0"
api_version = "1"
side = "both"
dependencies = ["core"]
permissions = ["world.read", "world.write"]
